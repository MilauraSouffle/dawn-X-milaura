name: CLA disabled pending supply-chain review

on:
  workflow_dispatch:

permissions: {}

jobs:
  security-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse privileged fork-triggered execution
        run: |
          echo "CLA workflow disabled on 2026-08-31 after supply-chain review."
          exit 1
