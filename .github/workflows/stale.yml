name: Stale workflow disabled pending supply-chain review

on:
  workflow_dispatch:

permissions: {}

jobs:
  security-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse unpinned third-party Action execution
        run: |
          echo "Stale workflow disabled on 2026-08-31 pending immutable Action pinning."
          exit 1
