name: CI disabled pending supply-chain review

on:
  workflow_dispatch:

permissions: {}

jobs:
  security-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse third-party Actions execution
        run: |
          echo "CI disabled on 2026-08-31 pending immutable Action pinning and least-privilege review."
          exit 1
