{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .milaura-reassurance {
    background-color: {{ section.settings.background_color }};
    border-top: 1px solid rgba(0,0,0,0.05);
    border-bottom: 1px solid rgba(0,0,0,0.05);
  }

  .reassurance-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }

  @media screen and (min-width: 750px) {
    .reassurance-grid {
      grid-template-columns: repeat(3, 1fr); /* Default 3 columns */
    }
    
    {% if section.blocks.size == 4 %}
    .reassurance-grid {
      grid-template-columns: repeat(4, 1fr);
    }
    {% endif %}
  }

  .reassurance-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    transition: transform 0.3s ease;
  }
  
  .reassurance-item:hover {
    transform: translateY(-5px);
  }

  .reassurance-icon {
    font-size: 3rem;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  
  .reassurance-content h4 {
    font-family: var(--milaura-font-heading);
    font-size: 1.8rem;
    margin: 0 0 0.5rem 0;
    color: var(--milaura-text);
  }
  
  .reassurance-content p {
    margin: 0;
    font-size: 1.4rem;
    color: var(--milaura-text-light);
    line-height: 1.4;
  }
{%- endstyle -%}

<div class="milaura-reassurance section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="reassurance-grid">
      {%- for block in section.blocks -%}
        <div class="reassurance-item" {{ block.shopify_attributes }}>
          <div class="reassurance-icon">
            {%- if block.settings.icon_type == 'emoji' -%}
              {{ block.settings.icon_emoji }}
            {%- else -%}
              {% render 'icon-accordion', icon: block.settings.icon_name %}
            {%- endif -%}
          </div>
          <div class="reassurance-content">
            <h4>{{ block.settings.title }}</h4>
            <p>{{ block.settings.description }}</p>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Milaura Reassurance",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Couleur de fond",
      "default": "#FDFBF7"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Élément",
      "limit": 4,
      "settings": [
        {
          "type": "select",
          "id": "icon_type",
          "label": "Type d'icône",
          "options": [
            { "value": "emoji", "label": "Emoji" },
            { "value": "icon", "label": "Icône Thème" }
          ],
          "default": "emoji"
        },
        {
          "type": "text",
          "id": "icon_emoji",
          "label": "Emoji",
          "default": "🚚"
        },
        {
          "type": "text",
          "id": "icon_name",
          "label": "Nom Icône (si type 'icon')",
          "info": "ex: shipping, return, etc."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Titre",
          "default": "Livraison Rapide"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Expédition sous 24/48h"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Milaura Reassurance",
      "blocks": [
        { "type": "item", "settings": { "icon_emoji": "🚚", "title": "Livraison Offerte", "description": "Dès 50€ d'achat" } },
        { "type": "item", "settings": { "icon_emoji": "🛡️", "title": "Garantie 30j", "description": "Satisfait ou remboursé" } },
        { "type": "item", "settings": { "icon_emoji": "💳", "title": "Paiement Sécurisé", "description": "100% sécurisé SSL" } }
      ]
    }
  ]
}
{% endschema %}
