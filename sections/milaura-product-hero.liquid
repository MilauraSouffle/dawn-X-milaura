{{ 'section-main-product.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}
{{ 'component-product-variant-picker.css' | asset_url | stylesheet_tag }}
{{ 'component-product-model.css' | asset_url | stylesheet_tag }}

{% style %}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* Layout Grid */
  .milaura-hero-wrapper {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--milaura-spacing-md);
  }

  @media screen and (min-width: 990px) {
    .milaura-hero-wrapper {
      grid-template-columns: 1.5fr 1fr;
      gap: var(--milaura-spacing-xl);
      align-items: start;
    }

    .milaura-hero-info-column {
      position: sticky;
      top: calc(var(--header-height-offset, 96px) + 24px);
      background: rgba(255,255,255,0.5); /* Subtle glass for contrast if needed */
      border-radius: var(--milaura-border-radius);
      padding: var(--milaura-spacing-md);
    }
  }

  /* Info Column Styling */
  .milaura-product__title {
    font-family: var(--milaura-font-heading);
    font-size: 3.2rem;
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }

  .milaura-product__price {
    font-size: 2.2rem;
    font-weight: 600;
    color: var(--milaura-gold);
    margin-bottom: 1.5rem;
  }

  /* Variant Tiles (Nutrimuscle Style) */
  .product-form__input input[type='radio'] + label {
    border-radius: 8px !important;
    padding: 1rem 1.5rem !important;
    font-weight: 600 !important;
    border: 1px solid rgba(0,0,0,0.1) !important;
    transition: all 0.2s ease;
    background: #fff;
    min-width: 60px;
    text-align: center;
  }

  .product-form__input input[type='radio']:checked + label {
    background-color: var(--milaura-gold) !important;
    color: #fff !important;
    border-color: var(--milaura-gold) !important;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(192, 160, 98, 0.2);
  }

  /* Quantity & Add Button Row */
  .milaura-buy-row {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
    align-items: center;
  }

  .milaura-add-btn {
    flex: 1;
    background: var(--milaura-gold);
    color: white;
    font-weight: 700;
    padding: 1.5rem;
    border-radius: 12px;
    border: none;
    cursor: pointer;
    font-size: 1.6rem;
    transition: all 0.3s;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .milaura-add-btn:hover {
    background: var(--milaura-gold-dark);
    transform: scale(1.02);
  }

  .milaura-add-btn:disabled {
    background: #ccc;
    cursor: not-allowed;
  }

  /* Trust Badges Inline */
  .milaura-trust-badges {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    font-size: 1.4rem;
    opacity: 0.9;
  }
  .milaura-trust-item {
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .milaura-trust-icon {
    color: var(--milaura-gold);
    font-size: 1.6rem;
  }

  /* Sticky Bar Mobile */
  .milaura-sticky-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: white;
    padding: 1.5rem;
    box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    transform: translateY(100%);
    transition: transform 0.3s ease-out;
    border-top: 1px solid var(--milaura-gold-light);
  }

  .milaura-sticky-bar.is-visible {
    transform: translateY(0);
  }
  
  .sticky-info h4 { margin: 0; font-size: 1.4rem; }
  .sticky-info span { color: var(--milaura-gold); font-weight: bold; }

  .sticky-btn {
    background: var(--milaura-gold);
    color: white;
    border: none;
    padding: 1rem 2rem;
    border-radius: 8px;
    font-weight: bold;
    cursor: pointer;
  }

  /* Volume Offer Cards */
  .milaura-volume-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    margin: 2rem 0;
  }
  .volume-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border: 1px solid rgba(0,0,0,0.1);
    border-radius: 12px;
    padding: 1.5rem;
    cursor: pointer;
    position: relative;
    background: #fff;
    transition: all 0.2s;
  }
  .volume-card:hover { border-color: var(--milaura-gold); }
  .volume-card.is-selected {
    border: 2px solid var(--milaura-gold);
    background: rgba(192, 160, 98, 0.05);
    transform: scale(1.02);
    box-shadow: 0 5px 15px rgba(192, 160, 98, 0.15);
  }
  .volume-card-badge {
    position: absolute;
    top: -12px;
    right: 20px;
    background: var(--milaura-gold);
    color: white;
    font-size: 1.1rem;
    padding: 4px 12px;
    border-radius: 20px;
    font-weight: bold;
    text-transform: uppercase;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  }
  .volume-left { display: flex; align-items: center; }
  .volume-check {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    border: 2px solid #ccc;
    margin-right: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .volume-card.is-selected .volume-check {
    border-color: var(--milaura-gold);
    background: var(--milaura-gold);
  }
  .volume-check::after {
    content: '';
    width: 10px;
    height: 10px;
    background: white;
    border-radius: 50%;
    display: none;
  }
  .volume-card.is-selected .volume-check::after { display: block; }
  .volume-text h5 { margin: 0; font-size: 1.6rem; font-weight: 700; }
  .volume-text p { margin: 0; font-size: 1.3rem; color: var(--milaura-text-light); }
  .volume-price { font-weight: 700; font-size: 1.6rem; text-align: right; }
{% endstyle %}

<script src="{{ 'product-info.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>

<div class="page-width section-{{ section.id }}-padding">
  <div class="milaura-hero-wrapper">
    
    <!-- LEFT: Gallery -->
    <div class="milaura-hero-gallery-column">
      {% render 'product-media-gallery', 
        product: product, 
        variant_images: section.settings.variant_images, 
        is_duplicate: false,
        gallery_layout: section.settings.gallery_layout 
      %}
    </div>

    <!-- RIGHT: Info & Buy -->
    <div class="milaura-hero-info-column">
      <product-info id="ProductInfo-{{ section.id }}" data-section="{{ section.id }}" data-url="{{ product.url }}">
        
        <!-- Vendor / Breadcrumb -->
        {%- if section.settings.show_vendor -%}
          <div class="product__vendor caption-with-letter-spacing">{{ product.vendor }}</div>
        {%- endif -%}

        <!-- Title -->
        <h1 class="milaura-product__title">{{ product.title | escape }}</h1>

        <!-- Reviews Badge -->
        <a href="#Reviews" class="rating-badge" style="display:inline-flex; align-items:center; gap:5px; margin-bottom:1rem; text-decoration:none; color:inherit;">
          <span style="color:var(--milaura-gold)">★★★★★</span>
          <span style="font-size:1.4rem; opacity:0.7;">(4.9/5)</span>
        </a>

        <!-- Price -->
        <div class="no-js-hidden" id="price-{{ section.id }}" role="status" {{ block.shopify_attributes }}>
          {%- render 'price', product: product, use_variant: true, show_badges: true, price_class: 'milaura-product__price' -%}
        </div>

        <!-- Variant Picker (Dawn Standard for logic, Nutrimuscle style for CSS) -->
        {%- unless product.has_only_default_variant -%}
          <variant-radios class="no-js-hidden" data-section="{{ section.id }}" data-url="{{ product.url }}" data-update-url="false">
            {%- for option in product.options_with_values -%}
              <fieldset class="js product-form__input">
                <legend class="form__label" style="font-weight:bold; margin-bottom:0.5rem;">{{ option.name }}</legend>
                <div style="display:flex; flex-wrap:wrap; gap:10px;">
                  {% render 'product-variant-options', product: product, option: option, block: block, picker_type: 'button' %}
                </div>
              </fieldset>
            {%- endfor -%}
            <script type="application/json">
              {{ product.variants | json }}
            </script>
          </variant-radios>
        {%- endunless -%}

        <!-- Volume Offer Block -->
        {%- for block in section.blocks -%}
          {%- if block.type == 'volume_offer' -%}
            <div class="milaura-volume-grid" {{ block.shopify_attributes }}>
              <!-- Option 1 -->
              <div class="volume-card is-selected" onclick="selectVolume(1, this)">
                <div class="volume-left">
                  <div class="volume-check"></div>
                  <div class="volume-text">
                    <h5>{{ block.settings.opt1_label }}</h5>
                    <p>{{ block.settings.opt1_sub }}</p>
                  </div>
                </div>
                <div class="volume-price">
                  {{ product.price | money }}
                </div>
              </div>

              <!-- Option 2 -->
              <div class="volume-card" onclick="selectVolume(2, this)">
                {% if block.settings.opt2_badge != blank %}
                  <div class="volume-card-badge">{{ block.settings.opt2_badge }}</div>
                {% endif %}
                <div class="volume-left">
                  <div class="volume-check"></div>
                  <div class="volume-text">
                    <h5>{{ block.settings.opt2_label }}</h5>
                    <p>{{ block.settings.opt2_sub }}</p>
                  </div>
                </div>
                <div class="volume-price">
                  {% assign price2 = product.price | times: 2 | times: 0.9 %}
                  {{ price2 | money }}
                </div>
              </div>

              <!-- Option 3 -->
              <div class="volume-card" onclick="selectVolume(3, this)">
                {% if block.settings.opt3_badge != blank %}
                  <div class="volume-card-badge">{{ block.settings.opt3_badge }}</div>
                {% endif %}
                <div class="volume-left">
                  <div class="volume-check"></div>
                  <div class="volume-text">
                    <h5>{{ block.settings.opt3_label }}</h5>
                    <p>{{ block.settings.opt3_sub }}</p>
                  </div>
                </div>
                <div class="volume-price">
                  {% assign price3 = product.price | times: 3 | times: 0.8 %}
                  {{ price3 | money }}
                </div>
              </div>
            </div>
            
            <script>
              function selectVolume(qty, card) {
                // 1. Visual Update
                document.querySelectorAll('.volume-card').forEach(c => c.classList.remove('is-selected'));
                card.classList.add('is-selected');
                
                // 2. Update Hidden Quantity Input
                const qtyInput = document.getElementById('Quantity-{{ section.id }}');
                if(qtyInput) {
                  qtyInput.value = qty;
                  // Trigger change event if needed by other scripts
                  qtyInput.dispatchEvent(new Event('change', { bubbles: true }));
                }
              }
            </script>
          {%- endif -%}
        {%- endfor -%}

        <!-- Buy Buttons -->
        <div class="milaura-buy-row">
          <!-- Quantity (Optional) -->
          {%- if section.settings.show_quantity -%}
            <div class="product-form__input product-form__quantity" style="width:100px;">
              <quantity-input class="quantity">
                <button class="quantity__button no-js-hidden" name="minus" type="button"><span class="visually-hidden">-</span>-</button>
                <input class="quantity__input" type="number" name="quantity" id="Quantity-{{ section.id }}" min="1" value="1" form="{{ 'product-form-' | append: section.id }}">
                <button class="quantity__button no-js-hidden" name="plus" type="button"><span class="visually-hidden">+</span>+</button>
              </quantity-input>
            </div>
          {%- endif -%}

          <!-- Add Button -->
          <product-form class="product-form" style="flex:1;">
            {%- form 'product', product, id: product_form_id, class: 'form', novalidate: 'novalidate', data-type: 'add-to-cart-form' -%}
              <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" disabled>
              <button
                type="submit"
                name="add"
                class="milaura-add-btn"
                {% if product.selected_or_first_available_variant.available == false %}disabled{% endif %}
              >
                <span>
                  {%- if product.selected_or_first_available_variant.available -%}
                    Ajouter au panier
                  {%- else -%}
                    Rupture de stock
                  {%- endif -%}
                </span>
              </button>
            {%- endform -%}
          </product-form>
        </div>

        <!-- Trust Badges (Reassurance) -->
        <div class="milaura-trust-badges">
          {%- for block in section.blocks -%}
            {%- if block.type == 'trust_badge' -%}
              <div class="milaura-trust-item">
                <span class="milaura-trust-icon">{{ block.settings.icon }}</span>
                <span>{{ block.settings.text }}</span>
              </div>
            {%- endif -%}
          {%- endfor -%}
        </div>

        <!-- Description Extract (Optional) -->
        {%- if section.settings.show_description -%}
          <div class="product__description rte quick-add-hidden" style="margin-top:2rem;">
            {{ product.description | strip_html | truncatewords: 30 }}
            <a href="#ProductTabs" style="text-decoration:underline; color:var(--milaura-gold);">Lire la suite</a>
          </div>
        {%- endif -%}

      </product-info>
    </div>
  </div>
</div>

<!-- Sticky Bottom Bar (Hidden by default) -->
<div id="MilauraStickyBar" class="milaura-sticky-bar">
  <div class="sticky-info">
    <h4>{{ product.title }}</h4>
    <span id="StickyPrice">
      {{ product.selected_or_first_available_variant.price | money }}
    </span>
  </div>
  <button class="sticky-btn" onclick="document.querySelector('.milaura-add-btn').click()">
    Ajouter
  </button>
</div>

<!-- Sticky Logic -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const stickyBar = document.getElementById('MilauraStickyBar');
    const mainButton = document.querySelector('.milaura-add-btn');
    const stickyPrice = document.getElementById('StickyPrice');
    
    // Show sticky bar when main button scrolls out of view
    if(stickyBar && mainButton) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (!entry.isIntersecting) {
            stickyBar.classList.add('is-visible');
          } else {
            stickyBar.classList.remove('is-visible');
          }
        });
      }, { threshold: 0 });
      
      observer.observe(mainButton);
    }
    
    // Update sticky price when main price changes (Variant change)
    // We listen to the price update event from Dawn or simple mutation observer
    const priceNode = document.querySelector('.milaura-product__price');
    if(priceNode && stickyPrice) {
      const observer = new MutationObserver(() => {
        stickyPrice.innerHTML = priceNode.innerHTML;
      });
      observer.observe(priceNode, { childList: true, subtree: true, characterData: true });
    }
  });
</script>

{% schema %}
{
  "name": "Milaura Hero Premium",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_vendor",
      "default": false,
      "label": "Afficher vendeur"
    },
    {
      "type": "checkbox",
      "id": "show_quantity",
      "default": true,
      "label": "Sélecteur quantité"
    },
    {
      "type": "select",
      "id": "gallery_layout",
      "options": [
        { "value": "stacked", "label": "Empilé" },
        { "value": "columns", "label": "Colonnes" },
        { "value": "thumbnail", "label": "Thumbnails" },
        { "value": "thumbnail_slider", "label": "Thumbnail Slider" }
      ],
      "default": "thumbnail",
      "label": "Layout Galerie"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "trust_badge",
      "name": "Badge Réassurance",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Emoji/Icône",
          "default": "🚚"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Texte",
          "default": "Livraison offerte"
        }
      ]
    },
    {
      "type": "volume_offer",
      "name": "Offre Volume (Bundle)",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "Affiche 3 cartes pour inciter à l'achat en volume. Les remises doivent être configurées dans Shopify (Réductions automatiques)."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Titre Section",
          "default": "Choisir mon offre"
        },
        {
          "type": "header",
          "content": "Option 1 (1 Unité)"
        },
        {
          "type": "text",
          "id": "opt1_label",
          "label": "Label",
          "default": "Découverte"
        },
        {
          "type": "text",
          "id": "opt1_sub",
          "label": "Sous-texte",
          "default": "Standard"
        },
        {
          "type": "header",
          "content": "Option 2 (2 Unités)"
        },
        {
          "type": "text",
          "id": "opt2_label",
          "label": "Label",
          "default": "Duo"
        },
        {
          "type": "text",
          "id": "opt2_sub",
          "label": "Sous-texte",
          "default": "Économisez 10%"
        },
        {
          "type": "text",
          "id": "opt2_badge",
          "label": "Badge",
          "default": "POPULAIRE"
        },
        {
          "type": "header",
          "content": "Option 3 (3 Unités)"
        },
        {
          "type": "text",
          "id": "opt3_label",
          "label": "Label",
          "default": "Cure Complète"
        },
        {
          "type": "text",
          "id": "opt3_sub",
          "label": "Sous-texte",
          "default": "Économisez 20%"
        },
        {
          "type": "text",
          "id": "opt3_badge",
          "label": "Badge",
          "default": "MEILLEURE OFFRE"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Milaura Hero Premium",
      "blocks": [
        { "type": "trust_badge", "settings": { "icon": "🚚", "text": "Livraison offerte dès 50€" } },
        { "type": "trust_badge", "settings": { "icon": "🛡️", "text": "Garantie 30 jours" } },
        { "type": "volume_offer" }
      ]
    }
  ]
}
{% endschema %}