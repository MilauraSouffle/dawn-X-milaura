{%- comment -%}
  MILAURA COLLECTION HERO - DYNAMIC ISLAND
  Animation style Apple Dynamic Island + BFM typewriter
  Ultra-minimal, 2026 design
  @version 4.0.0
{%- endcomment -%}

{%- style -%}
  #MilauraCollectionHero-{{ section.id }} {
    padding: var(--milaura-spacing-xs) var(--milaura-spacing-sm) var(--milaura-spacing-xs);
  }

  /* =============================================
     BREADCRUMB - Ultra discret
     ============================================= */
  #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    margin-bottom: 12px;
    font-size: 11px;
    color: var(--milaura-text-light, #999);
  }

  #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb a {
    color: inherit;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb a:hover {
    color: var(--milaura-gold, #C0A062);
  }

  #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb svg {
    width: 8px;
    height: 8px;
    opacity: 0.4;
  }

  #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb span {
    color: var(--milaura-text, #333);
  }

  /* =============================================
     DYNAMIC ISLAND - La magie
     ============================================= */
  #MilauraCollectionHero-{{ section.id }} .hero__island-wrapper {
    display: flex;
    justify-content: center;
    margin-bottom: 8px;
  }

  #MilauraCollectionHero-{{ section.id }} .hero__island {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;

    /* État initial : petit point */
    min-width: 12px;
    height: 42px;
    padding: 0 20px;

    /* Style noir premium */
    background: linear-gradient(145deg, #1a1a1a 0%, #0d0d0d 100%);
    border-radius: 50px;

    /* Liseré doré subtil */
    box-shadow:
      0 0 0 1px rgba(192, 160, 98, 0.25),
      0 8px 32px rgba(0, 0, 0, 0.25),
      0 2px 8px rgba(0, 0, 0, 0.15),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);

    /* Animation d'expansion */
    animation: islandExpand 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
    overflow: hidden;
  }

  @keyframes islandExpand {
    0% {
      min-width: 12px;
      padding: 0 6px;
      opacity: 0.5;
      transform: scale(0.8);
    }
    30% {
      min-width: 12px;
      padding: 0 6px;
      opacity: 1;
      transform: scale(1);
    }
    100% {
      min-width: auto;
      padding: 0 20px;
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Lueur dorée au hover */
  #MilauraCollectionHero-{{ section.id }} .hero__island::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 52px;
    background: linear-gradient(135deg,
      rgba(192, 160, 98, 0.4) 0%,
      transparent 40%,
      transparent 60%,
      rgba(192, 160, 98, 0.4) 100%
    );
    opacity: 0;
    transition: opacity 0.4s ease;
    z-index: -1;
  }

  #MilauraCollectionHero-{{ section.id }} .hero__island:hover::before {
    opacity: 1;
  }

  /* Indicateur pulsant (point doré) */
  #MilauraCollectionHero-{{ section.id }} .hero__island-dot {
    width: 6px;
    height: 6px;
    background: var(--milaura-gold, #C0A062);
    border-radius: 50%;
    flex-shrink: 0;
    animation: dotPulse 2s ease-in-out infinite;
    box-shadow: 0 0 8px rgba(192, 160, 98, 0.6);
  }

  @keyframes dotPulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
      box-shadow: 0 0 8px rgba(192, 160, 98, 0.6);
    }
    50% {
      opacity: 0.7;
      transform: scale(0.8);
      box-shadow: 0 0 4px rgba(192, 160, 98, 0.3);
    }
  }

  /* Titre avec effet typewriter */
  #MilauraCollectionHero-{{ section.id }} .hero__island-title {
    font-family: {{ section.settings.title_font }};
    font-size: {{ section.settings.title_size }}px;
    font-weight: 500;
    color: #ffffff;
    margin: 0;
    white-space: nowrap;
    overflow: hidden;
    letter-spacing: 0.5px;
  }

  #MilauraCollectionHero-{{ section.id }} .hero__island-title .typewriter {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    animation: typing 1.2s steps(30, end) 0.3s forwards;
    width: 0;
  }

  @keyframes typing {
    from { width: 0; }
    to { width: 100%; }
  }

  /* Curseur clignotant (optionnel) */
  #MilauraCollectionHero-{{ section.id }} .hero__island-title .cursor {
    display: inline-block;
    width: 2px;
    height: 1em;
    background: var(--milaura-gold, #C0A062);
    margin-left: 2px;
    animation: blink 0.8s infinite;
    vertical-align: middle;
    opacity: 0;
    animation: cursorFade 1.5s forwards;
  }

  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  @keyframes cursorFade {
    0%, 80% { opacity: 0; }
    81%, 100% { opacity: 1; animation: blink 0.8s infinite; }
  }

  /* Compteur produits */
  #MilauraCollectionHero-{{ section.id }} .hero__island-count {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 4px 10px;
    background: rgba(192, 160, 98, 0.2);
    border-radius: 20px;
    font-size: 11px;
    font-weight: 600;
    color: var(--milaura-gold-light, #E6C88B);
    flex-shrink: 0;
    opacity: 0;
    animation: fadeIn 0.4s ease 1s forwards;
  }

  @keyframes fadeIn {
    to { opacity: 1; }
  }

  /* =============================================
     RESPONSIVE
     ============================================= */
  @media screen and (max-width: 768px) {
    #MilauraCollectionHero-{{ section.id }} {
      padding: var(--milaura-spacing-xs) var(--milaura-spacing-xs) var(--milaura-spacing-xs);
    }

    #MilauraCollectionHero-{{ section.id }} .hero__breadcrumb {
      font-size: 10px;
      margin-bottom: 10px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island {
      height: 38px;
      padding: 0 16px;
      gap: 8px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-title {
      font-size: {{ section.settings.title_size | times: 0.75 | round: 0 }}px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-count {
      padding: 3px 8px;
      font-size: 10px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-dot {
      width: 5px;
      height: 5px;
    }
  }

  /* Petit mobile */
  @media screen and (max-width: 380px) {
    #MilauraCollectionHero-{{ section.id }} .hero__island {
      height: 34px;
      padding: 0 12px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-title {
      font-size: 16px;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-count {
      display: none;
    }
  }

  /* =============================================
     REDUCED MOTION
     ============================================= */
  @media (prefers-reduced-motion: reduce) {
    #MilauraCollectionHero-{{ section.id }} .hero__island {
      animation: none;
      min-width: auto;
      padding: 0 20px;
      opacity: 1;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-title .typewriter {
      animation: none;
      width: auto;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-title .cursor {
      display: none;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-dot {
      animation: none;
    }

    #MilauraCollectionHero-{{ section.id }} .hero__island-count {
      animation: none;
      opacity: 1;
    }
  }
{%- endstyle -%}

<section
  id="MilauraCollectionHero-{{ section.id }}"
  class="milaura-collection-hero"
  aria-label="{{ collection.title | escape }}"
>
  <div class="page-width">
    {%- comment -%} Breadcrumb ultra-discret {%- endcomment -%}
    {%- if section.settings.show_breadcrumb -%}
      <nav class="hero__breadcrumb" aria-label="Fil d'Ariane">
        <a href="{{ routes.root_url }}">Accueil</a>
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
        <span aria-current="page">{{ collection.title }}</span>
      </nav>
    {%- endif -%}

    {%- comment -%} Dynamic Island {%- endcomment -%}
    <div class="hero__island-wrapper">
      <h1 class="hero__island" role="banner">
        {%- comment -%} Point doré pulsant {%- endcomment -%}
        <span class="hero__island-dot" aria-hidden="true"></span>

        {%- comment -%} Titre avec typewriter {%- endcomment -%}
        <span class="hero__island-title">
          <span class="typewriter">{{ collection.title }}</span>
          {%- if section.settings.show_cursor -%}
            <span class="cursor" aria-hidden="true"></span>
          {%- endif -%}
        </span>

        {%- comment -%} Compteur {%- endcomment -%}
        {%- if section.settings.show_count -%}
          <span class="hero__island-count">
            {{ collection.products_count }} {{ collection.products_count | pluralize: 'item', 'items' }}
          </span>
        {%- endif -%}
      </h1>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Collection Hero",
  "tag": "section",
  "class": "milaura-section",
  "settings": [
    {
      "type": "header",
      "content": "📝 Contenu"
    },
    {
      "type": "checkbox",
      "id": "show_breadcrumb",
      "label": "Afficher le breadcrumb",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_count",
      "label": "Afficher le compteur",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_cursor",
      "label": "Afficher le curseur clignotant",
      "default": true,
      "info": "Effet machine à écrire"
    },
    {
      "type": "header",
      "content": "✏️ Style"
    },
    {
      "type": "select",
      "id": "title_font",
      "label": "Police du titre",
      "options": [
        { "value": "'Lato', sans-serif", "label": "Lato" },
        { "value": "'Playfair Display', serif", "label": "Playfair Display" },
        { "value": "'Dancing Script', cursive", "label": "Dancing Script" }
      ],
      "default": "'Lato', sans-serif"
    },
    {
      "type": "range",
      "id": "title_size",
      "label": "Taille du titre",
      "min": 16,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Collection Hero"
    }
  ]
}
{% endschema %}
