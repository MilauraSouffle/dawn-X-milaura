{% style %}
  /* ============================================
     MILAURA BENEFITS EXPLORER - V6 (FLUID MOBILE)
     ============================================ */

  #MilauraBenefits-{{ section.id }} {
    width: 100%;
    /* Padding vertical pour que l'image de fond respire */
    padding: var(--milaura-spacing-lg) 0;
    background: transparent;
    margin-top: var(--milaura-spacing-lg);
    margin-bottom: var(--milaura-spacing-lg);
    transition: background-image 0.5s ease-in-out;
    background-size: cover;
    background-position: center;
    position: relative;
  }

  /* --- CONTENEUR GLOBAL (LA CARTE) --- */
  #MilauraBenefits-{{ section.id }} .benefits-dashboard {
    overflow: hidden;
    position: relative;
    z-index: 2;
    /* Style Halo Vision OS Desktop */
    border: 1px solid rgba(192, 160, 98, 0.4);
    box-shadow: 0 10px 40px rgba(192, 160, 98, 0.15);
    border-radius: 30px;
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    margin: 0 auto;
    max-width: 1400px;
    width: 92%;
    padding: var(--milaura-spacing-md);
  }

  #MilauraBenefits-{{ section.id }} .benefits-header {
    text-align: center;
    margin-bottom: 40px;
  }

  /* --- LAYOUT GRID DESKTOP --- */
  #MilauraBenefits-{{ section.id }} .benefits-layout {
    display: grid;
    grid-template-columns: 350px 1fr;
    gap: 40px;
    align-items: start;
  }

  /* --- NAVIGATION GAUCHE (DESKTOP) --- */
  #MilauraBenefits-{{ section.id }} .benefits-nav {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn {
    background: rgba(255, 255, 255, 0.5);
    border: 1px solid transparent;
    border-radius: 20px;
    padding: 20px;
    text-align: left;
    cursor: pointer;
    transition: all 0.3s ease;
    width: 100%;
    position: relative;
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateX(5px);
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn.is-active {
    background: #ffffff;
    border-color: var(--milaura-gold);
    box-shadow: 0 10px 25px rgba(192, 160, 98, 0.15);
    transform: scale(1.02);
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn-content {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn-icon {
    font-size: 1.8rem;
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn-name {
    font-family: var(--milaura-font-heading);
    font-weight: 700;
    color: var(--milaura-text);
    font-size: 1.5rem;
    display: block;
  }

  #MilauraBenefits-{{ section.id }} .benefit-btn-tagline {
    font-family: var(--milaura-font-body);
    font-size: 1.1rem;
    color: var(--milaura-text-light);
    display: block;
    margin-top: 6px;
  }

  /* --- ZONE DROITE (DESKTOP) --- */
  #MilauraBenefits-{{ section.id }} .benefits-content {
    display: grid;
    grid-template-areas: "stack";
    border-radius: 25px;
    overflow: hidden;
    position: relative;
    min-height: 500px;
    box-shadow: inset 0 0 0 1px rgba(255,255,255,0.3);
  }

  #MilauraBenefits-{{ section.id }} .benefit-card {
    grid-area: stack;
    opacity: 0;
    z-index: 0;
    pointer-events: none;
    transition: opacity 0.5s ease-in-out, transform 0.5s ease;
    transform: scale(0.98);
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    padding: var(--milaura-spacing-md);
  }

  #MilauraBenefits-{{ section.id }} .benefit-card.is-active {
    opacity: 1;
    z-index: 1;
    pointer-events: auto;
    transform: scale(1);
  }

  /* Overlay desktop */
  #MilauraBenefits-{{ section.id }} .benefit-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, rgba(255,255,255,0.5) 0%, rgba(255,255,255,0.2) 60%, rgba(255,255,255,0) 100%);
    z-index: 1;
  }

  /* --- BOITE TEXTE --- */
  #MilauraBenefits-{{ section.id }} .benefit-info-box {
    position: relative;
    z-index: 2;
    max-width: 480px;
    background: rgba(255, 255, 255, 0.65);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    padding: var(--milaura-spacing-md);
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  #MilauraBenefits-{{ section.id }} .benefit-info-title {
    font-family: var(--milaura-font-heading);
    font-size: 2.2rem;
    color: var(--milaura-text);
    margin-bottom: 10px;
    line-height: 1.2;
  }

  #MilauraBenefits-{{ section.id }} .benefit-info-subtitle {
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 1.1rem;
    color: var(--milaura-gold);
    font-weight: 700;
    margin-bottom: 20px;
  }

  #MilauraBenefits-{{ section.id }} .benefit-info-desc {
    font-family: var(--milaura-font-body);
    color: var(--milaura-text);
    font-size: 1.1rem;
    line-height: 1.7;
  }

  /* --- PRODUIT MINI --- */
  #MilauraBenefits-{{ section.id }} .benefit-product-mini {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-top: 25px;
    padding-top: 20px;
    border-top: 1px solid rgba(0,0,0,0.1);
    flex-wrap: wrap;
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-info {
    flex: 1;
    min-width: 0;
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-title {
    font-weight: 700;
    margin-bottom: 5px;
    color: var(--milaura-text);
    font-size: 1.3rem;
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-price {
    color: var(--milaura-gold);
    font-weight: 700;
    font-size: 1.4rem;
  }

  /* Layout uniquement - les styles visuels viennent de .milaura-cta--small */
  #MilauraBenefits-{{ section.id }} .benefit-product-cta {
    flex-shrink: 0;
    white-space: nowrap;
    margin-left: auto;
<<<<<<< HEAD
    /* Neutralisation des styles visuels pour utiliser le design system global */
    padding: 0;
    font-size: inherit;
    box-shadow: none;
    background: none;
    border: none;
    border-radius: 0;
=======
  }

  /* Application du design system avec spécificité suffisante */
  #MilauraBenefits-{{ section.id }} .benefit-product-cta.milaura-cta--small {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 40px;
    min-width: 140px;
    padding: 10px 20px;
    font-size: 0.9rem;
    border-radius: 999px;
    border: 1px solid rgba(214, 176, 96, 0.55);
    background: linear-gradient(
      135deg,
      rgba(255, 240, 200, 0.92) 0%,
      rgba(214, 176, 96, 0.92) 18%,
      rgba(255, 232, 172, 0.92) 42%,
      rgba(176, 130, 48, 0.92) 70%,
      rgba(255, 240, 200, 0.92) 100%
    );
    color: #fff;
    text-decoration: none;
    font-weight: 700;
    letter-spacing: 0.3px;
    box-shadow:
      0 12px 32px rgba(130, 90, 30, 0.15),
      0 0 0 1px rgba(255, 255, 255, 0.25) inset;
    transition: transform 180ms ease, box-shadow 220ms ease;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    font-family: inherit;
    -webkit-appearance: none;
    appearance: none;
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-cta.milaura-cta--small::after {
    content: '';
    position: absolute;
    inset: -40% -60%;
    background: linear-gradient(
      120deg,
      transparent 0%,
      rgba(255, 255, 255, 0.50) 35%,
      transparent 70%
    );
    opacity: 0;
    transform: translateX(-18%);
    transition: opacity 260ms ease, transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
    pointer-events: none;
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-cta.milaura-cta--small:hover {
    transform: translateY(-1px);
    box-shadow:
      0 16px 40px rgba(130, 90, 30, 0.20),
      0 0 0 1px rgba(255, 255, 255, 0.30) inset,
      0 0 24px rgba(214, 176, 96, 0.28);
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-cta.milaura-cta--small:hover::after {
    opacity: 0.35;
    transform: translateX(18%);
  }

  @media screen and (min-width: 769px) {
    #MilauraBenefits-{{ section.id }} .benefit-product-cta.milaura-cta--small {
      min-width: 160px;
      padding: 11px 24px;
      font-size: 0.95rem;
    }
>>>>>>> fdfd993 (feat: update milaura ui)
  }

  #MilauraBenefits-{{ section.id }} .benefit-product-thumb {
    width: 70px; height: 70px;
    border-radius: 12px;
    object-fit: cover;
    flex-shrink: 0;
  }

  #MilauraBenefits-{{ section.id }} .benefits-dots {
    display: none;
  }

  /* ============================================
     MOBILE / TABLETTE (≤ 990px)
     ============================================ */
  @media screen and (max-width: 990px) {
    #MilauraBenefits-{{ section.id }} {
      padding: var(--milaura-spacing-sm) 0;
    }

    #MilauraBenefits-{{ section.id }}::before {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(255, 255, 255, 0.35); /* Un peu plus opaque pour lisibilité */
      z-index: 1;
      pointer-events: none;
    }

    /* Cadre mobile */
    #MilauraBenefits-{{ section.id }} .benefits-dashboard {
      width: 95%;
      margin: 0 auto;
      background: rgba(255, 255, 255, 0.75) !important;
      border: 1px solid rgba(192, 160, 98, 0.4) !important;
      box-shadow: 0 10px 30px rgba(192, 160, 98, 0.15) !important;
      padding: 20px 0;
      border-radius: 20px;
    }

    #MilauraBenefits-{{ section.id }} .benefits-layout {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    #MilauraBenefits-{{ section.id }} .benefits-header {
      padding: 0 20px;
      margin-bottom: 10px;
    }

    /* --- CARROUSEL NAV (FIX SCROLL) --- */
    #MilauraBenefits-{{ section.id }} .benefits-nav {
      display: flex;
      flex-direction: row;
      flex-wrap: nowrap; /* Force la ligne unique */
      overflow-x: auto; /* Active le scroll */
      -webkit-overflow-scrolling: touch; /* Scroll natif iOS */
      scroll-behavior: smooth;

      /* On retire scroll-snap strict qui bloque parfois le slide fluide */
      scroll-snap-type: none;

      /* Padding latéral pour que le premier élément ne soit pas collé */
      padding: 10px 20px 20px 20px;

      gap: 15px;
      justify-content: flex-start; /* Important : aligne à gauche pour permettre le scroll */
      width: 100%;

      /* Cache la scrollbar */
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    #MilauraBenefits-{{ section.id }} .benefits-nav::-webkit-scrollbar {
      display: none;
    }

    #MilauraBenefits-{{ section.id }} .benefit-btn {
      min-width: 85%; /* Largeur confortable */
      width: 85%;
      flex-shrink: 0; /* Empêche l'écrasement */
      background: rgba(255, 255, 255, 0.95);
      border: 1px solid rgba(192, 160, 98, 0.3);
      box-shadow: 0 5px 15px rgba(0,0,0,0.05);
      margin: 0;
      /* On s'assure que le clic marche */
      pointer-events: auto;
      touch-action: pan-x; /* Dit au navigateur : "laisse le doigt scroller horizontalement" */
    }

    #MilauraBenefits-{{ section.id }} .benefit-btn.is-active {
      background: #ffffff;
      border-color: var(--milaura-gold);
      border-width: 2px;
      transform: none;
      box-shadow: 0 8px 25px rgba(192, 160, 98, 0.2);
    }

    /* --- DOTS (CENTRAGE FORCÉ) --- */
    #MilauraBenefits-{{ section.id }} .benefits-dots {
      display: flex;
      width: 100%;
      justify-content: center; /* Centre horizontalement */
      align-items: center;
      gap: 8px;
      margin-top: 5px;
      margin-bottom: 20px;
      padding: 0;
    }

    #MilauraBenefits-{{ section.id }} .benefit-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: rgba(192, 160, 98, 0.3);
      border: none;
      padding: 0;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    #MilauraBenefits-{{ section.id }} .benefit-dot.is-active {
      background: var(--milaura-gold);
      width: 24px;
      border-radius: 10px;
    }

    /* --- CONTENU --- */
    #MilauraBenefits-{{ section.id }} .benefits-content {
      display: block;
      min-height: auto;
      padding: 0 20px;
      background: transparent;
      box-shadow: none;
      overflow: visible;
    }

    #MilauraBenefits-{{ section.id }} .benefit-card {
      display: none;
      padding: 0;
      background: none !important;
    }

    #MilauraBenefits-{{ section.id }} .benefit-card.is-active {
      display: block;
      opacity: 1;
      transform: none;
    }

    #MilauraBenefits-{{ section.id }} .benefit-card::before {
      display: none;
    }

    #MilauraBenefits-{{ section.id }} .benefit-info-box {
      width: 100%;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(192, 160, 98, 0.2);
      box-shadow: 0 10px 30px rgba(192, 160, 98, 0.1);
    }

    /* Layout mobile uniquement - les styles visuels viennent de .milaura-cta--small */
    #MilauraBenefits-{{ section.id }} .benefit-product-cta {
      width: 100%;
      text-align: center;
      margin-top: 15px;
      /* Les styles visuels (padding, font-size, etc.) viennent du design system global */
    }
  }
{% endstyle %}

<div id="MilauraBenefits-{{ section.id }}">
  <div class="benefits-dashboard">
    {% if section.settings.title != blank %}
      <div class="benefits-header">
        <h2 class="milaura-heading">{{ section.settings.title }}</h2>
        {% if section.settings.subtitle != blank %}
          <div class="benefits-subtitle milaura-text">{{ section.settings.subtitle }}</div>
        {% endif %}
      </div>
    {% endif %}

    <div class="benefits-layout">
      <nav class="benefits-nav" role="tablist" id="benefits-nav-{{ section.id }}">
        {%- for block in section.blocks -%}
          <button
            type="button"
            id="benefit-btn-{{ block.id }}"
            class="benefit-btn {% if forloop.first %}is-active{% endif %}"
            data-target="benefit-{{ block.id }}"
            data-index="{{ forloop.index0 }}"
            data-bg="{{ block.settings.stone_image | image_url: width: 1200 }}"
            role="tab"
            aria-selected="{% if forloop.first %}true{% else %}false{% endif %}"
            aria-controls="benefit-{{ block.id }}"
            {{ block.shopify_attributes }}
          >
            <div class="benefit-btn-content">
              <span class="benefit-btn-icon">{{ block.settings.icon }}</span>
              <div>
                <span class="benefit-btn-name">{{ block.settings.emotion }}</span>
                <span class="benefit-btn-tagline">{{ block.settings.tagline }}</span>
              </div>
            </div>
          </button>
        {%- endfor -%}
      </nav>

      <div class="benefits-dots" id="benefits-dots-{{ section.id }}">
        {%- for block in section.blocks -%}
          <button
            type="button"
            class="benefit-dot {% if forloop.first %}is-active{% endif %}"
            data-index="{{ forloop.index0 }}"
            aria-label="Voir le besoin {{ forloop.index }}"
          ></button>
        {%- endfor -%}
      </div>

      <div class="benefits-content">
        {%- for block in section.blocks -%}
          <div
            id="benefit-{{ block.id }}"
            class="benefit-card {% if forloop.first %}is-active{% endif %}"
            role="tabpanel"
            aria-labelledby="benefit-btn-{{ block.id }}"
            style="background-image: url('{{ block.settings.stone_image | image_url: width: 1400 }}');"
          >
            <div class="benefit-info-box">
              <div class="benefit-info-subtitle">{{ block.settings.stone_subtitle }}</div>
              <h3 class="benefit-info-title">{{ block.settings.stone_name }}</h3>
              <div class="benefit-info-desc milaura-text">{{ block.settings.description }}</div>

              {%- if block.settings.product != blank -%}
                {% assign prod = block.settings.product %}
                <div class="benefit-product-mini">
                  {% if prod.featured_image %}
                    {{ prod.featured_image | image_url: width: 140 | image_tag: class: 'benefit-product-thumb' }}
                  {% endif %}
                  <div class="benefit-product-info">
                    <div class="benefit-product-title">{{ prod.title }}</div>
                    <div class="benefit-product-price">{{ prod.price | money }}</div>
                  </div>
                  <a href="{{ prod.url }}" class="milaura-cta milaura-cta--small benefit-product-cta">
                    {{ block.settings.product_cta_label | default: 'Je veux cette pierre' }}
                  </a>
                </div>
              {%- endif -%}
            </div>
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const sectionId = '{{ section.id }}';
    const container = document.querySelector('#MilauraBenefits-' + sectionId);
    const nav = document.querySelector('#benefits-nav-' + sectionId);
    const btns = nav ? nav.querySelectorAll('.benefit-btn') : [];
    const dots = container.querySelectorAll('.benefit-dot');
    const cards = container.querySelectorAll('.benefit-card');

    if (!container || btns.length === 0) return;

    // --- FONCTIONS ---

    function updateBackground(url) {
      if (window.innerWidth <= 990 && url) {
        container.style.backgroundImage = `url('${url}')`;
      } else {
        container.style.backgroundImage = 'none';
      }
    }

    function activateTab(index) {
      if (index < 0 || index >= btns.length) return;

      btns.forEach((b) => {
        b.classList.remove('is-active');
        b.setAttribute('aria-selected', 'false');
      });
      dots.forEach((d) => d.classList.remove('is-active'));
      cards.forEach((c) => c.classList.remove('is-active'));

      if (btns[index]) {
        btns[index].classList.add('is-active');
        btns[index].setAttribute('aria-selected', 'true');
        updateBackground(btns[index].dataset.bg);
      }
      if (dots[index]) dots[index].classList.add('is-active');
      if (cards[index]) cards[index].classList.add('is-active');
    }

    function centerNavOnItem(index) {
      if (window.innerWidth > 990 || !nav || !btns[index]) return;

      const btn = btns[index];
      const navWidth = nav.offsetWidth;
      const btnWidth = btn.offsetWidth;
      const btnLeft = btn.offsetLeft;

      // Calcul propre pour centrer sans faire sauter la page
      const scrollPos = btnLeft - navWidth / 2 + btnWidth / 2;

      nav.scrollTo({
        left: scrollPos,
        behavior: 'smooth',
      });
    }

    // --- EVENTS ---

    // 1. Clic sur Bouton (Nav)
    btns.forEach((btn, i) => {
      btn.addEventListener('click', () => {
        activateTab(i);
        centerNavOnItem(i);
      });
    });

    // 2. Clic sur Dot
    dots.forEach((dot, i) => {
      dot.addEventListener('click', () => {
        activateTab(i);
        centerNavOnItem(i);
      });
    });

    // 3. Init
    const activeBtn = nav.querySelector('.benefit-btn.is-active');
    if (activeBtn) {
      const index = parseInt(activeBtn.dataset.index);
      updateBackground(activeBtn.dataset.bg);
    } else {
      activateTab(0);
    }

    // 4. Resize Reset
    window.addEventListener('resize', () => {
      const active = nav.querySelector('.benefit-btn.is-active');
      if (active) updateBackground(active.dataset.bg);
    });
  });
</script>

{% schema %}
{
  "name": "Milaura Benefits Explorer",
  "settings": [
    { "type": "text", "id": "title", "label": "Titre Section", "default": "De quoi as-tu besoin ?" },
    { "type": "text", "id": "subtitle", "label": "Sous-titre" }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Besoin",
      "settings": [
        { "type": "text", "id": "icon", "label": "Emoji", "default": "💎" },
        { "type": "text", "id": "emotion", "label": "Nom du besoin", "default": "Stress" },
        { "type": "text", "id": "tagline", "label": "Tagline", "default": "Pour s'apaiser" },
        { "type": "image_picker", "id": "stone_image", "label": "Image de fond (Pierre)" },
        { "type": "text", "id": "stone_name", "label": "Nom de la Pierre", "default": "Améthyste" },
        { "type": "text", "id": "stone_subtitle", "label": "Sous-titre", "default": "Sérénité & Calme" },
        { "type": "richtext", "id": "description", "label": "Description" },
        { "type": "product", "id": "product", "label": "Produit Lié" },
        { "type": "text", "id": "product_cta_label", "label": "Bouton", "default": "Découvrir" }
      ]
    }
  ],
  "presets": [{ "name": "Milaura Benefits Explorer" }]
}
{% endschema %}
