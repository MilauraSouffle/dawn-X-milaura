{% comment %}
  MILAURA FOOTER 2.0
  Footer premium avec collections auto-generees, social proof, et trust badges
  Style Vision OS glassmorphism
{% endcomment %}

<footer id="MilauraFooter-{{ section.id }}" class="milaura-footer">
  <div class="milaura-footer-container">

    {%- comment -%} TOP : Logo + Newsletter {%- endcomment -%}
    <div class="milaura-footer-top">
      <div class="milaura-footer-brand">
        {% if section.settings.logo != blank %}
          <img src="{{ section.settings.logo | image_url: width: 200 }}" alt="{{ shop.name }}" class="milaura-footer-logo">
        {% else %}
          <h2 class="milaura-footer-logo-text">{{ shop.name }}</h2>
        {% endif %}

        {% if section.settings.tagline != blank %}
          <p class="milaura-footer-tagline">{{ section.settings.tagline }}</p>
        {% endif %}
      </div>

      {%- if section.settings.show_newsletter -%}
        <div class="milaura-footer-newsletter">
          <h3 class="milaura-footer-newsletter-title">{{ section.settings.newsletter_title }}</h3>
          <p class="milaura-footer-newsletter-text">{{ section.settings.newsletter_text }}</p>

          <form method="post" action="/contact#contact_form" accept-charset="UTF-8" class="milaura-newsletter-form">
            <input type="hidden" name="form_type" value="customer">
            <input type="hidden" name="utf8" value="✓">
            <input type="hidden" name="contact[tags]" value="newsletter">

            <div class="milaura-newsletter-input-wrapper">
              <label for="milaura-newsletter-email-{{ section.id }}" class="visually-hidden">
                Adresse email pour la newsletter
              </label>
              <input
                type="email"
                id="milaura-newsletter-email-{{ section.id }}"
                name="contact[email]"
                placeholder="{{ section.settings.newsletter_placeholder }}"
                class="milaura-newsletter-input"
                autocomplete="email"
                required
              >
              <button type="submit" class="milaura-newsletter-btn">
                {{ section.settings.newsletter_button }}
              </button>
            </div>
          </form>
        </div>
      {%- endif -%}
    </div>

    {%- comment -%} MIDDLE : Colonnes principales {%- endcomment -%}
    <div class="milaura-footer-middle">

      {%- comment -%} Colonne 1 : Collections auto-generees (filtre les collections internes) {%- endcomment -%}
      {%- if section.settings.show_collections -%}
        <div class="milaura-footer-column">
          <h3 class="milaura-footer-column-title">{{ section.settings.collections_title }}</h3>
          <ul class="milaura-footer-links">
            {%- for collection in collections -%}
              {%- comment -%} Exclure collections internes/techniques {%- endcomment -%}
              {%- assign is_internal = false -%}
              {%- if collection.handle contains 'recos' or collection.handle contains 'pool' or collection.handle contains 'frontpage' or collection.handle contains 'all' -%}
                {%- assign is_internal = true -%}
              {%- endif -%}
              {%- if collection.all_products_count > 0 and is_internal == false -%}
                <li>
                  <a href="{{ collection.url }}" class="milaura-footer-link">
                    {{ collection.title }}
                  </a>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
      {%- endif -%}

      {%- comment -%} Colonnes dynamiques via blocks {%- endcomment -%}
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'link_list' -%}
            <div class="milaura-footer-column" {{ block.shopify_attributes }}>
              <h3 class="milaura-footer-column-title">{{ block.settings.heading }}</h3>

              {%- if block.settings.menu != blank -%}
                <ul class="milaura-footer-links">
                  {%- for link in linklists[block.settings.menu].links -%}
                    <li>
                      <a href="{{ link.url }}" class="milaura-footer-link">
                        {{ link.title }}
                      </a>
                    </li>
                  {%- endfor -%}
                </ul>
              {%- endif -%}
            </div>

          {%- when 'text' -%}
            <div class="milaura-footer-column" {{ block.shopify_attributes }}>
              <h3 class="milaura-footer-column-title">{{ block.settings.heading }}</h3>
              <div class="milaura-footer-text">
                {{ block.settings.text }}
              </div>
            </div>
        {%- endcase -%}
      {%- endfor -%}

      {%- comment -%} Colonne : Decouvrir (avec blog placeholder) {%- endcomment -%}
      {%- if section.settings.show_discover -%}
        <div class="milaura-footer-column">
          <h3 class="milaura-footer-column-title">{{ section.settings.discover_title }}</h3>
          <ul class="milaura-footer-links">
            {%- if section.settings.discover_quiz_link != blank -%}
              <li>
                <a href="{{ section.settings.discover_quiz_link }}" class="milaura-footer-link">
                  {{ section.settings.discover_quiz_label }}
                </a>
              </li>
            {%- endif -%}
            {%- if section.settings.discover_blog_link != blank -%}
              <li>
                <a href="{{ section.settings.discover_blog_link }}" class="milaura-footer-link">
                  {{ section.settings.discover_blog_label }}
                </a>
              </li>
            {%- endif -%}
            {%- if section.settings.discover_about_link != blank -%}
              <li>
                <a href="{{ section.settings.discover_about_link }}" class="milaura-footer-link">
                  {{ section.settings.discover_about_label }}
                </a>
              </li>
            {%- endif -%}
          </ul>
        </div>
      {%- endif -%}

      {%- comment -%} Colonne : Reseaux sociaux avec SVG {%- endcomment -%}
      {%- if section.settings.show_social -%}
        <div class="milaura-footer-column">
          <h3 class="milaura-footer-column-title">{{ section.settings.social_title }}</h3>
          <div class="milaura-footer-social">
            {%- if settings.social_instagram_link != blank -%}
              <a href="{{ settings.social_instagram_link }}" class="milaura-social-link" target="_blank" rel="noopener" aria-label="Suivez-nous sur Instagram">
                <svg class="milaura-social-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
                  <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/>
                  <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
                </svg>
              </a>
            {%- endif -%}
            {%- if settings.social_facebook_link != blank -%}
              <a href="{{ settings.social_facebook_link }}" class="milaura-social-link" target="_blank" rel="noopener" aria-label="Suivez-nous sur Facebook">
                <svg class="milaura-social-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>
                </svg>
              </a>
            {%- endif -%}
            {%- if settings.social_tiktok_link != blank -%}
              <a href="{{ settings.social_tiktok_link }}" class="milaura-social-link" target="_blank" rel="noopener" aria-label="Suivez-nous sur TikTok">
                <svg class="milaura-social-svg" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-5.2 1.74 2.89 2.89 0 0 1 2.31-4.64 2.93 2.93 0 0 1 .88.13V9.4a6.84 6.84 0 0 0-1-.05A6.33 6.33 0 0 0 5 20.1a6.34 6.34 0 0 0 10.86-4.43v-7a8.16 8.16 0 0 0 4.77 1.52v-3.4a4.85 4.85 0 0 1-1-.1z"/>
                </svg>
              </a>
            {%- endif -%}
            {%- if settings.social_pinterest_link != blank -%}
              <a href="{{ settings.social_pinterest_link }}" class="milaura-social-link" target="_blank" rel="noopener" aria-label="Suivez-nous sur Pinterest">
                <svg class="milaura-social-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"/>
                  <path d="M8 12c0-4 3-5 4.5-5 2 0 3.5 1.5 3.5 3.5 0 2.5-1.5 4.5-3 4.5s-2-1-2-1"/>
                  <path d="M9.5 14.5l-1 6"/>
                </svg>
              </a>
            {%- endif -%}
            {%- if settings.social_youtube_link != blank -%}
              <a href="{{ settings.social_youtube_link }}" class="milaura-social-link" target="_blank" rel="noopener" aria-label="Suivez-nous sur YouTube">
                <svg class="milaura-social-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.33z"/>
                  <polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"/>
                </svg>
              </a>
            {%- endif -%}
          </div>
        </div>
      {%- endif -%}
    </div>

    {%- comment -%} TRUST BADGES (style reassurance, plus petits) {%- endcomment -%}
    {%- if section.settings.show_trust_badges -%}
      <div class="milaura-footer-trust">
        <div class="milaura-footer-trust-grid">
          {%- if section.settings.trust_1_text != blank -%}
            <div class="milaura-footer-trust-item">
              <span class="milaura-footer-trust-emoji">{{ section.settings.trust_1_emoji }}</span>
              <span class="milaura-footer-trust-text">{{ section.settings.trust_1_text }}</span>
            </div>
          {%- endif -%}
          {%- if section.settings.trust_2_text != blank -%}
            <div class="milaura-footer-trust-item">
              <span class="milaura-footer-trust-emoji">{{ section.settings.trust_2_emoji }}</span>
              <span class="milaura-footer-trust-text">{{ section.settings.trust_2_text }}</span>
            </div>
          {%- endif -%}
          {%- if section.settings.trust_3_text != blank -%}
            <div class="milaura-footer-trust-item">
              <span class="milaura-footer-trust-emoji">{{ section.settings.trust_3_emoji }}</span>
              <span class="milaura-footer-trust-text">{{ section.settings.trust_3_text }}</span>
            </div>
          {%- endif -%}
          {%- if section.settings.trust_4_text != blank -%}
            <div class="milaura-footer-trust-item">
              <span class="milaura-footer-trust-emoji">{{ section.settings.trust_4_emoji }}</span>
              <span class="milaura-footer-trust-text">{{ section.settings.trust_4_text }}</span>
            </div>
          {%- endif -%}
        </div>
      </div>
    {%- endif -%}

    {%- comment -%} SOCIAL PROOF PLACEHOLDER {%- endcomment -%}
    {%- if section.settings.show_social_proof -%}
      <div class="milaura-footer-social-proof">
        <div class="milaura-footer-social-proof-content">
          {%- if section.settings.social_proof_stars -%}
            <div class="milaura-footer-stars">
              {%- for i in (1..5) -%}
                <svg class="milaura-star-icon" viewBox="0 0 24 24" fill="currentColor">
                  <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                </svg>
              {%- endfor -%}
            </div>
          {%- endif -%}
          <p class="milaura-footer-social-proof-text">{{ section.settings.social_proof_text }}</p>
        </div>
      </div>
    {%- endif -%}

    {%- comment -%} BOTTOM : Legal + Paiements {%- endcomment -%}
    <div class="milaura-footer-bottom">
      <div class="milaura-footer-legal">
        <p class="milaura-footer-copyright">
          © {{ 'now' | date: '%Y' }} {{ shop.name }}. {{ section.settings.copyright_text }}
        </p>

        {%- if section.settings.show_policy_links -%}
          <ul class="milaura-footer-policies">
            {%- if shop.privacy_policy -%}
              <li><a href="{{ shop.privacy_policy.url }}">{{ shop.privacy_policy.title }}</a></li>
            {%- endif -%}
            {%- if shop.terms_of_service -%}
              <li><a href="{{ shop.terms_of_service.url }}">{{ shop.terms_of_service.title }}</a></li>
            {%- endif -%}
            {%- if shop.refund_policy -%}
              <li><a href="{{ shop.refund_policy.url }}">{{ shop.refund_policy.title }}</a></li>
            {%- endif -%}
          </ul>
        {%- endif -%}
      </div>

      {%- if section.settings.show_payment_icons -%}
        <div class="milaura-footer-payment">
          <span class="milaura-footer-payment-text">{{ section.settings.payment_text }}</span>
          {%- unless shop.enabled_payment_types == empty -%}
            <ul class="milaura-payment-icons">
              {%- for type in shop.enabled_payment_types -%}
                <li class="milaura-payment-icon">{{ type | payment_type_svg_tag }}</li>
              {%- endfor -%}
            </ul>
          {%- endunless -%}
        </div>
      {%- endif -%}
    </div>

  </div>
</footer>

<style>
  /* --- FOOTER MILAURA 2.0 VISION OS --- */
  .milaura-footer {
    background: linear-gradient(
      135deg,
      {{ section.settings.bg_color_start }} 0%,
      {{ section.settings.bg_color_end }} 100%
    );
    border-top: 1px solid rgba(192, 160, 98, 0.2);
    padding: 3rem 0 1.5rem 0;
    margin-top: 0;
  }

  .milaura-footer-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 var(--milaura-spacing-sm, 12px);
  }

  /* --- TOP : Logo + Newsletter --- */
  .milaura-footer-top {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(192, 160, 98, 0.12);
    margin-bottom: 2rem;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-top {
      grid-template-columns: 1fr 1fr;
      align-items: start;
    }
  }

  .milaura-footer-brand {
    text-align: left;
  }

  .milaura-footer-logo {
    max-width: 140px;
    height: auto;
    margin-bottom: 0.75rem;
  }

  .milaura-footer-logo-text {
    font-family: var(--font-heading-family, 'Playfair Display'), serif;
    font-size: 1.75rem;
    color: var(--milaura-gold, #C0A062);
    margin: 0 0 0.75rem 0;
  }

  .milaura-footer-tagline {
    font-size: {{ section.settings.tagline_size }}px;
    color: {{ section.settings.text_color }};
    margin: 0;
    line-height: 1.6;
    max-width: 360px;
  }

  /* Newsletter */
  .milaura-footer-newsletter {
    background: rgba(255, 255, 255, 0.45);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 16px;
    padding: 1.5rem;
    border: 1px solid rgba(192, 160, 98, 0.15);
  }

  .milaura-footer-newsletter-title {
    font-family: var(--font-heading-family, 'Playfair Display'), serif;
    font-size: {{ section.settings.newsletter_title_size }}px;
    margin: 0 0 0.35rem 0;
    color: {{ section.settings.title_color }};
  }

  .milaura-footer-newsletter-text {
    font-size: {{ section.settings.newsletter_text_size }}px;
    margin: 0 0 1rem 0;
    color: {{ section.settings.text_color }};
  }

  .milaura-newsletter-input-wrapper {
    display: flex;
    gap: 0.5rem;
  }

  .milaura-newsletter-input {
    flex: 1;
    padding: 0.75rem 1rem;
    border: 1px solid rgba(192, 160, 98, 0.25);
    border-radius: 50px;
    background: rgba(255, 255, 255, 0.7);
    font-size: 0.875rem;
    transition: all 0.3s ease;
  }

  .milaura-newsletter-input:focus {
    outline: none;
    border-color: var(--milaura-gold, #C0A062);
    box-shadow: 0 0 0 3px rgba(192, 160, 98, 0.1);
  }

  .milaura-newsletter-btn {
    padding: 0.75rem 1.5rem;
    background: var(--milaura-gold, #C0A062);
    color: white;
    border: none;
    border-radius: 50px;
    font-weight: 600;
    font-size: 0.875rem;
    cursor: pointer;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .milaura-newsletter-btn:hover {
    background: var(--milaura-gold-dark, #8F723A);
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(192, 160, 98, 0.25);
  }

  /* --- MIDDLE : Colonnes de liens --- */
  .milaura-footer-middle {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(192, 160, 98, 0.12);
    margin-bottom: 1.5rem;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-middle {
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 2rem;
    }
  }

  .milaura-footer-column-title {
    font-family: var(--font-heading-family, 'Playfair Display'), serif;
    font-size: {{ section.settings.column_title_size }}px;
    margin: 0 0 0.75rem 0;
    color: {{ section.settings.title_color }};
  }

  .milaura-footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .milaura-footer-links li {
    margin-bottom: 0.5rem;
  }

  .milaura-footer-link {
    color: {{ section.settings.text_color }};
    text-decoration: none;
    font-size: {{ section.settings.link_size }}px;
    transition: color 0.3s ease;
  }

  .milaura-footer-link:hover {
    color: {{ section.settings.link_hover_color }};
  }

  .milaura-footer-text {
    font-size: {{ section.settings.link_size }}px;
    line-height: 1.6;
    color: {{ section.settings.text_color }};
  }

  /* Reseaux sociaux avec SVG */
  .milaura-footer-social {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .milaura-social-link {
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.5);
    border-radius: 50%;
    border: 1px solid rgba(192, 160, 98, 0.2);
    text-decoration: none;
    color: {{ section.settings.text_color }};
    transition: all 0.3s ease;
  }

  .milaura-social-link:hover {
    background: var(--milaura-gold, #C0A062);
    border-color: var(--milaura-gold, #C0A062);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(192, 160, 98, 0.25);
  }

  .milaura-social-svg {
    width: 18px;
    height: 18px;
  }

  /* --- TRUST BADGES (mini version) --- */
  .milaura-footer-trust {
    padding: 1.25rem 0;
    border-bottom: 1px solid rgba(192, 160, 98, 0.12);
    margin-bottom: 1.5rem;
  }

  .milaura-footer-trust-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-trust-grid {
      justify-content: space-around;
    }
  }

  .milaura-footer-trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .milaura-footer-trust-emoji {
    font-size: {{ section.settings.trust_emoji_size }}px;
  }

  .milaura-footer-trust-text {
    font-size: {{ section.settings.trust_text_size }}px;
    color: {{ section.settings.text_color }};
    font-weight: 500;
  }

  /* --- SOCIAL PROOF --- */
  .milaura-footer-social-proof {
    padding: 1.25rem 0;
    border-bottom: 1px solid rgba(192, 160, 98, 0.12);
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .milaura-footer-social-proof-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-social-proof-content {
      flex-direction: row;
      justify-content: center;
      gap: 0.75rem;
    }
  }

  .milaura-footer-stars {
    display: flex;
    gap: 0.15rem;
  }

  .milaura-star-icon {
    width: 16px;
    height: 16px;
    color: var(--milaura-gold, #C0A062);
  }

  .milaura-footer-social-proof-text {
    font-size: {{ section.settings.social_proof_text_size }}px;
    color: {{ section.settings.text_color }};
    margin: 0;
  }

  /* --- BOTTOM : Legal + Paiements --- */
  .milaura-footer-bottom {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    text-align: center;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-bottom {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
    }
  }

  .milaura-footer-legal {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-legal {
      flex-direction: row;
      align-items: center;
      gap: 1.5rem;
    }
  }

  .milaura-footer-copyright {
    font-size: 0.8125rem;
    color: rgba(0, 0, 0, 0.55);
    margin: 0;
  }

  .milaura-footer-policies {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-policies {
      justify-content: flex-start;
    }
  }

  .milaura-footer-policies a {
    color: rgba(0, 0, 0, 0.55);
    text-decoration: none;
    font-size: 0.8125rem;
    transition: color 0.3s ease;
  }

  .milaura-footer-policies a:hover {
    color: var(--milaura-gold, #C0A062);
  }

  /* Icones de paiement */
  .milaura-footer-payment {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  @media screen and (min-width: 768px) {
    .milaura-footer-payment {
      justify-content: flex-end;
    }
  }

  .milaura-footer-payment-text {
    font-size: 0.8125rem;
    color: rgba(0, 0, 0, 0.55);
  }

  .milaura-payment-icons {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: 0.35rem;
    flex-wrap: wrap;
  }

  .milaura-payment-icon {
    width: 36px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .milaura-payment-icon svg {
    width: 100%;
    height: auto;
    opacity: 0.65;
  }

  /* Mobile adjustments */
  @media screen and (max-width: 767px) {
    .milaura-footer {
      padding: 2rem 0 1.25rem 0;
    }

    .milaura-footer-top {
      gap: 1.5rem;
      padding-bottom: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .milaura-newsletter-input-wrapper {
      flex-direction: column;
    }

    .milaura-newsletter-btn {
      width: 100%;
    }

    .milaura-footer-trust-grid {
      gap: 1rem;
    }

    .milaura-footer-trust-item {
      flex-basis: calc(50% - 0.5rem);
      justify-content: center;
    }
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .milaura-social-link,
    .milaura-newsletter-btn,
    .milaura-footer-link {
      transition: none;
    }
  }
</style>

{% schema %}
{
  "name": "Milaura Footer 2.0",
  "tag": "footer",
  "class": "milaura-footer-section",
  "settings": [
    {
      "type": "header",
      "content": "🎨 Couleurs"
    },
    {
      "type": "color",
      "id": "bg_color_start",
      "label": "Fond degrade debut",
      "default": "#FDFBF7"
    },
    {
      "type": "color",
      "id": "bg_color_end",
      "label": "Fond degrade fin",
      "default": "#F2E8D5"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Couleur des titres",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Couleur du texte",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "link_hover_color",
      "label": "Couleur liens au survol",
      "default": "#C0A062"
    },
    {
      "type": "header",
      "content": "🖼️ Logo & Branding"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "textarea",
      "id": "tagline",
      "label": "Tagline",
      "default": "Bougies et pierres naturelles pour creer des rituels emotionnels authentiques."
    },
    {
      "type": "range",
      "id": "tagline_size",
      "label": "Taille tagline",
      "min": 12,
      "max": 18,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright text",
      "default": "Tous droits reserves."
    },
    {
      "type": "header",
      "content": "📧 Newsletter"
    },
    {
      "type": "checkbox",
      "id": "show_newsletter",
      "label": "Afficher newsletter",
      "default": true
    },
    {
      "type": "text",
      "id": "newsletter_title",
      "label": "Titre newsletter",
      "default": "Rejoins notre communaute"
    },
    {
      "type": "range",
      "id": "newsletter_title_size",
      "label": "Taille titre newsletter",
      "min": 16,
      "max": 28,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "textarea",
      "id": "newsletter_text",
      "label": "Texte newsletter",
      "default": "Recois en avant-premiere nos nouvelles creations et rituels exclusifs."
    },
    {
      "type": "range",
      "id": "newsletter_text_size",
      "label": "Taille texte newsletter",
      "min": 12,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "text",
      "id": "newsletter_placeholder",
      "label": "Placeholder email",
      "default": "ton@email.com"
    },
    {
      "type": "text",
      "id": "newsletter_button",
      "label": "Bouton newsletter",
      "default": "S'inscrire"
    },
    {
      "type": "header",
      "content": "🛍️ Collections auto"
    },
    {
      "type": "checkbox",
      "id": "show_collections",
      "label": "Afficher toutes les collections",
      "default": true
    },
    {
      "type": "text",
      "id": "collections_title",
      "label": "Titre colonne collections",
      "default": "Nos collections"
    },
    {
      "type": "header",
      "content": "🔍 Decouvrir"
    },
    {
      "type": "checkbox",
      "id": "show_discover",
      "label": "Afficher colonne Decouvrir",
      "default": true
    },
    {
      "type": "text",
      "id": "discover_title",
      "label": "Titre colonne",
      "default": "Decouvrir"
    },
    {
      "type": "text",
      "id": "discover_quiz_label",
      "label": "Lien quiz - Label",
      "default": "Le quiz emotionnel"
    },
    {
      "type": "url",
      "id": "discover_quiz_link",
      "label": "Lien quiz - URL"
    },
    {
      "type": "text",
      "id": "discover_blog_label",
      "label": "Lien blog - Label",
      "default": "Notre univers"
    },
    {
      "type": "url",
      "id": "discover_blog_link",
      "label": "Lien blog - URL"
    },
    {
      "type": "text",
      "id": "discover_about_label",
      "label": "Lien a propos - Label",
      "default": "Notre histoire"
    },
    {
      "type": "url",
      "id": "discover_about_link",
      "label": "Lien a propos - URL"
    },
    {
      "type": "header",
      "content": "📱 Reseaux sociaux"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "label": "Afficher reseaux sociaux",
      "default": true
    },
    {
      "type": "text",
      "id": "social_title",
      "label": "Titre reseaux",
      "default": "Suivez-nous"
    },
    {
      "type": "header",
      "content": "✨ Trust badges"
    },
    {
      "type": "checkbox",
      "id": "show_trust_badges",
      "label": "Afficher trust badges",
      "default": true
    },
    {
      "type": "range",
      "id": "trust_emoji_size",
      "label": "Taille emoji",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "trust_text_size",
      "label": "Taille texte badge",
      "min": 11,
      "max": 15,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "text",
      "id": "trust_1_emoji",
      "label": "Badge 1 - Emoji",
      "default": "🇫🇷"
    },
    {
      "type": "text",
      "id": "trust_1_text",
      "label": "Badge 1 - Texte",
      "default": "Artisanat Francais"
    },
    {
      "type": "text",
      "id": "trust_2_emoji",
      "label": "Badge 2 - Emoji",
      "default": "💎"
    },
    {
      "type": "text",
      "id": "trust_2_text",
      "label": "Badge 2 - Texte",
      "default": "Pierres Certifiees"
    },
    {
      "type": "text",
      "id": "trust_3_emoji",
      "label": "Badge 3 - Emoji",
      "default": "🚚"
    },
    {
      "type": "text",
      "id": "trust_3_text",
      "label": "Badge 3 - Texte",
      "default": "Livraison Offerte"
    },
    {
      "type": "text",
      "id": "trust_4_emoji",
      "label": "Badge 4 - Emoji",
      "default": "💝"
    },
    {
      "type": "text",
      "id": "trust_4_text",
      "label": "Badge 4 - Texte",
      "default": "Emballage Cadeau"
    },
    {
      "type": "header",
      "content": "⭐ Social Proof"
    },
    {
      "type": "checkbox",
      "id": "show_social_proof",
      "label": "Afficher social proof",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "social_proof_stars",
      "label": "Afficher etoiles",
      "default": true
    },
    {
      "type": "text",
      "id": "social_proof_text",
      "label": "Texte social proof",
      "default": "Rejoins + de 500 ames connectees"
    },
    {
      "type": "range",
      "id": "social_proof_text_size",
      "label": "Taille texte",
      "min": 12,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 13
    },
    {
      "type": "header",
      "content": "📏 Typographie colonnes"
    },
    {
      "type": "range",
      "id": "column_title_size",
      "label": "Taille titres colonnes",
      "min": 14,
      "max": 22,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "link_size",
      "label": "Taille liens",
      "min": 12,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "header",
      "content": "⚖️ Liens legaux & Paiements"
    },
    {
      "type": "checkbox",
      "id": "show_policy_links",
      "label": "Afficher liens legaux",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "Afficher icones de paiement",
      "default": true
    },
    {
      "type": "text",
      "id": "payment_text",
      "label": "Texte paiement",
      "default": "Paiement securise"
    }
  ],
  "blocks": [
    {
      "type": "link_list",
      "name": "Menu",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Titre",
          "default": "Aide"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu",
          "default": "footer"
        }
      ]
    },
    {
      "type": "text",
      "name": "Texte",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Titre",
          "default": "A propos"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Contenu",
          "default": "<p>Milaura cree des bougies emotionnelles et des bijoux en pierres naturelles pour accompagner tes rituels quotidiens.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Milaura Footer 2.0",
      "blocks": [
        {
          "type": "link_list",
          "settings": {
            "heading": "Aide",
            "menu": "footer"
          }
        }
      ]
    }
  ]
}
{% endschema %}
