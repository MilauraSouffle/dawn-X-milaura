{% comment %}
  MILAURA HERO HOMEPAGE
  Mobile-first product hero for the featured weekly stone.
  No JavaScript. Product, copy, benefits and trust badges stay editable.
{% endcomment %}

{%- assign product = section.settings.product -%}
{%- assign hero_image = section.settings.image -%}
{%- if hero_image == blank and product != blank and product.featured_image -%}
  {%- assign hero_image = product.featured_image -%}
{%- endif -%}
{%- assign cta_url = section.settings.cta_url | default: product.url | default: '/collections/bijoux-pierres-naturelles' -%}
{%- assign hero_label = section.settings.pretitle | default: section.settings.headline | strip_html | escape -%}
{%- assign headline_line_1 = section.settings.headline_line_1 | default: 'Je suis' -%}
{%- assign headline_line_2 = section.settings.headline_line_2 | default: 'la bague' -%}
{%- assign headline_line_3 = section.settings.headline_line_3 | default: "de l'ete" -%}

{% style %}
  #MilauraHeroHome-{{ section.id }} {
    --hero-ink: #10232c;
    --hero-muted: #315461;
    --hero-aqua: #9de7ee;
    --hero-aqua-strong: #49afc6;
    --hero-aqua-deep: #1e718c;
    --hero-pearl: #fbfff9;
    --hero-cream: #fff4df;
    --hero-gold: #c0a062;
    --hero-gold-soft: #f1d59a;
    --hero-line: rgba(255, 255, 255, 0.55);
    --hero-shadow: 0 28px 70px rgba(16, 35, 44, 0.24);
    position: relative;
    overflow: hidden;
    color: var(--hero-ink);
    background:
      linear-gradient(180deg, rgba(137, 226, 235, 0.98) 0%, rgba(89, 199, 217, 0.96) 38%, rgba(203, 232, 221, 0.94) 62%, rgba(246, 221, 171, 0.94) 84%, var(--milaura-surface-warm, #fff7e8) 100%);
    letter-spacing: 0;
    isolation: isolate;
  }

  #MilauraHeroHome-{{ section.id }}::before,
  #MilauraHeroHome-{{ section.id }}::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: -1;
  }

  #MilauraHeroHome-{{ section.id }}::before {
    background:
      linear-gradient(116deg, rgba(255, 255, 255, 0.82) 0 13%, transparent 13% 100%),
      linear-gradient(151deg, transparent 0 48%, rgba(255, 255, 255, 0.28) 48% 52%, transparent 52% 100%),
      repeating-linear-gradient(168deg, rgba(255, 255, 255, 0.16) 0 1px, transparent 1px 28px);
    opacity: 0.72;
  }

  #MilauraHeroHome-{{ section.id }}::after {
    background:
      linear-gradient(180deg, transparent 0 50%, rgba(255, 247, 228, 0.38) 72%, var(--milaura-surface-warm, #fff7e8) 100%),
      linear-gradient(92deg, transparent 0 62%, rgba(16, 35, 44, 0.18) 62% 100%);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-shell {
    min-height: calc(100svh - var(--header-height-mobile, 80px) - 170px);
    padding: 16px 18px 28px;
    display: grid;
    align-items: stretch;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-inner {
    width: min(100%, 1340px);
    margin: 0 auto;
    display: grid;
    grid-template-rows: minmax(194px, auto) auto;
    align-content: start;
    gap: 8px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual-col {
    position: relative;
    order: 1;
    min-height: 194px;
    display: grid;
    place-items: center;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual {
    position: relative;
    width: min(100%, 304px);
    aspect-ratio: 1 / 0.92;
    display: grid;
    place-items: center;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual::before {
    content: "";
    position: absolute;
    width: 86%;
    height: 58%;
    left: 7%;
    bottom: 2%;
    background:
      linear-gradient(180deg, rgba(255, 248, 229, 0.94), rgba(211, 177, 111, 0.45)),
      repeating-linear-gradient(90deg, rgba(16, 35, 44, 0.08) 0 1px, transparent 1px 13px);
    border: 1px solid rgba(255, 255, 255, 0.58);
    border-radius: 8px;
    transform: perspective(540px) rotateX(58deg) rotateZ(-7deg);
    box-shadow: 0 24px 48px rgba(16, 35, 44, 0.20);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual::after {
    content: "";
    position: absolute;
    width: 64%;
    height: 1px;
    right: 1%;
    top: 17%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.86), transparent);
    transform: rotate(-10deg);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-img {
    position: relative;
    z-index: 2;
    width: min(86%, 252px);
    height: auto;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border-radius: 10px;
    box-shadow: var(--hero-shadow);
    transform: rotate(-5deg);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-stone-tag {
    position: absolute;
    z-index: 3;
    right: 0;
    bottom: 10px;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    min-height: 36px;
    padding: 8px 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.84);
    color: var(--hero-ink);
    font: 700 0.72rem/1 var(--milaura-font-body);
    box-shadow: 0 12px 28px rgba(16, 35, 44, 0.16);
    backdrop-filter: blur(14px);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-stone-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--hero-aqua), #ffffff 48%, var(--hero-gold-soft));
    border: 1px solid rgba(16, 35, 44, 0.12);
    flex: 0 0 auto;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-content {
    position: relative;
    z-index: 2;
    order: 2;
    align-self: end;
    padding: 0 0 2px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-badge-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    margin-bottom: 10px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-badge,
  #MilauraHeroHome-{{ section.id }} .hero-home-stock {
    display: inline-flex;
    align-items: center;
    min-height: 28px;
    padding: 7px 11px;
    border-radius: 999px;
    border: 1px solid rgba(16, 35, 44, 0.13);
    background: rgba(255, 255, 255, 0.58);
    color: var(--hero-ink);
    font: 800 0.68rem/1 var(--milaura-font-body);
    text-transform: uppercase;
    backdrop-filter: blur(14px);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-pretitle {
    margin: 0 0 4px;
    max-width: 18rem;
    color: var(--hero-ink);
    font: 700 0.92rem/1.22 var(--milaura-font-body);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-headline {
    margin: 0;
    color: var(--hero-ink);
    font-family: var(--milaura-font-heading);
    font-size: 2.62rem;
    font-weight: 600;
    line-height: 0.94;
    text-wrap: balance;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-headline span {
    color: var(--hero-aqua-deep);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
    max-width: 34rem;
    margin: 9px 0 0;
    color: var(--hero-muted);
    font: 500 0.8rem/1.36 var(--milaura-font-body);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-benefits {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 6px;
    margin: 11px 0 0;
    padding: 0;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-benefits li {
    display: grid;
    grid-template-columns: 1fr;
    align-items: center;
    justify-items: center;
    gap: 5px;
    min-height: 54px;
    padding: 7px 5px;
    border: 1px solid rgba(255, 255, 255, 0.48);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.24);
    color: var(--hero-ink);
    font: 800 0.62rem/1.14 var(--milaura-font-body);
    text-align: center;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-benefit-icon {
    display: grid;
    place-items: center;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    border: 1px solid rgba(255, 255, 255, 0.72);
    background: rgba(255, 255, 255, 0.42);
    box-shadow: 0 8px 18px rgba(16, 35, 44, 0.10);
    font-size: 0.9rem;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-action-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 9px;
    margin-top: 11px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price {
    display: flex;
    align-items: baseline;
    gap: 8px;
    color: var(--hero-ink);
    font-family: var(--milaura-font-body);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price-now {
    font-size: 1.12rem;
    font-weight: 900;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price-label {
    color: var(--hero-muted);
    font-size: 0.78rem;
    font-weight: 700;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price-was {
    color: rgba(16, 35, 44, 0.48);
    font-size: 0.86rem;
    font-weight: 700;
    text-decoration: line-through;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    justify-self: start;
    min-height: 48px;
    width: min(100%, 252px);
    padding: 14px 18px;
    border: 1px solid rgba(255, 255, 255, 0.62);
    border-radius: 999px;
    background: linear-gradient(180deg, var(--milaura-cta-gold-top, #fff7e5), var(--milaura-cta-gold-bottom, #f0c777));
    color: var(--milaura-cta-ink, #2c210f);
    font: 900 0.96rem/1 var(--milaura-font-body);
    text-decoration: none;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 16px 34px rgba(50, 80, 90, 0.22);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-cta:hover {
    transform: translateY(-2px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 20px 42px rgba(50, 80, 90, 0.28);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-cta:focus-visible {
      outline: 3px solid rgba(16, 35, 44, 0.62);
    outline-offset: 4px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-cta svg {
    width: 17px;
    height: 17px;
    flex: 0 0 auto;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-trust {
    display: none;
    flex-wrap: wrap;
    gap: 8px 12px;
    margin-top: 9px;
    color: rgba(16, 35, 44, 0.82);
    font: 800 0.72rem/1.2 var(--milaura-font-body);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-trust span {
    display: inline-flex;
    align-items: center;
    gap: 5px;
  }

  @media (max-width: 379px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      min-height: calc(100svh - var(--header-height-mobile, 80px) - 150px);
      padding-left: 13px;
      padding-right: 13px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      grid-template-rows: minmax(184px, auto) auto;
      gap: 7px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: 2.28rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
      font-size: 0.8rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-benefits li {
      min-height: 52px;
      font-size: 0.58rem;
    }
  }

  @media (max-height: 740px) and (max-width: 767px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      min-height: auto;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      grid-template-rows: minmax(188px, auto) auto;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-benefits {
      gap: 4px;
      margin-top: 10px;
    }
  }

  @media (min-width: 768px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      min-height: calc(100svh - var(--header-height-tablet, 88px) - 64px);
      padding: 34px 32px 40px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      grid-template-columns: minmax(0, 0.86fr) minmax(390px, 1.14fr);
      grid-template-rows: 1fr;
      align-content: center;
      align-items: center;
      gap: 32px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-content {
      order: 1;
      align-self: center;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-visual-col {
      order: 2;
      min-height: 470px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-visual {
      width: min(100%, 610px);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-img {
      width: min(86%, 510px);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-stone-tag {
      right: 4%;
      bottom: 30px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-pretitle {
      max-width: 28rem;
      font-size: 1.22rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: 4.85rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
      font-size: 1.02rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-benefits {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 10px;
      max-width: 610px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-benefits li {
      grid-template-columns: 36px 1fr;
      gap: 9px;
      justify-items: start;
      min-height: 54px;
      padding-right: 8px;
      padding-top: 0;
      padding-bottom: 0;
      padding-left: 0;
      border: 0;
      background: transparent;
      text-align: left;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-benefit-icon {
      width: 36px;
      height: 36px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-action-row {
      grid-template-columns: auto 1fr;
      align-items: center;
      gap: 18px;
      margin-top: 22px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-cta {
      width: auto;
      min-width: 236px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-trust {
      display: flex;
    }
  }

  @media (min-width: 1100px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      min-height: calc(100svh - var(--header-height-desktop, 110px) - 72px);
      padding: 42px 56px 54px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      grid-template-columns: minmax(410px, 0.82fr) minmax(560px, 1.18fr);
      gap: 54px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-visual-col {
      min-height: 630px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-visual {
      width: min(100%, 760px);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-img {
      width: min(86%, 635px);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-stone-tag {
      right: 7%;
      bottom: 42px;
      min-height: 42px;
      padding: 9px 15px;
      font-size: 0.78rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: 5.95rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
      max-width: 31rem;
    }
  }

  @media (min-width: 1400px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      padding-left: 70px;
      padding-right: 70px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: 6.35rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #MilauraHeroHome-{{ section.id }} .hero-home-cta {
      transition: none;
    }
  }

  #shopify-section-{{ section.id }}.milaura-hero-homepage {
    margin-top: calc(-1 * var(--milaura-hero-overlap-mobile, 306px)) !important;
    overflow: hidden;
  }

  body:has(#MilauraHeroHome-{{ section.id }}) main#MainContent {
    padding-top: 0 !important;
  }

  #MilauraHeroHome-{{ section.id }} {
    min-height: 100svh;
    background: #bceff1;
  }

  #MilauraHeroHome-{{ section.id }}::before {
    z-index: 1;
    background:
      linear-gradient(180deg, rgba(189, 241, 244, 0.08) 0%, rgba(189, 241, 244, 0.06) 36%, rgba(199, 240, 237, 0.76) 63%, rgba(255, 244, 218, 0.96) 100%),
      linear-gradient(90deg, rgba(8, 35, 43, 0.34) 0%, rgba(8, 35, 43, 0.06) 44%, rgba(255, 248, 230, 0.84) 100%);
    opacity: 1;
  }

  #MilauraHeroHome-{{ section.id }}::after {
    z-index: 1;
    background:
      radial-gradient(circle at 78% 22%, rgba(255, 255, 255, 0.76) 0 12%, transparent 32%),
      linear-gradient(112deg, transparent 0 52%, rgba(255, 248, 226, 0.70) 52% 100%);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-shell {
    position: relative;
    z-index: 2;
    min-height: 100svh;
    padding: calc(var(--header-height-mobile, 118px) + 22px) 18px calc(104px + env(safe-area-inset-bottom));
    display: grid;
    align-items: end;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-inner {
    display: block;
    width: min(100%, 1380px);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual-col {
    position: absolute;
    inset: 0;
    z-index: 0;
    min-height: 0;
    display: block;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual {
    width: 100%;
    height: 100%;
    aspect-ratio: auto;
    display: block;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-picture {
    display: block;
    width: 100%;
    height: 100%;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-visual::before,
  #MilauraHeroHome-{{ section.id }} .hero-home-visual::after {
    display: none;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-img {
    width: 100%;
    height: 100%;
    max-width: none;
    aspect-ratio: auto;
    object-fit: cover;
    object-position: center center;
    border-radius: 0;
    box-shadow: none;
    transform: none;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-stone-tag {
    right: 18px;
    top: calc(var(--header-height-mobile, 118px) + 96px);
    bottom: auto;
    min-height: 34px;
    padding: 8px 12px;
    border: 1px solid rgba(255, 255, 255, 0.70);
    background: rgba(255, 255, 255, 0.76);
    font-size: 0.7rem;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-ring-cutout {
    display: none;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-content {
    width: min(100%, 420px);
    padding: 0;
    color: var(--hero-ink);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-badge-row,
  #MilauraHeroHome-{{ section.id }} .hero-home-benefits {
    display: none;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-pretitle {
    max-width: none;
    margin: 0 0 10px;
    color: rgba(16, 35, 44, 0.78);
    font: 900 0.72rem/1.1 var(--milaura-font-body);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-headline {
    display: grid;
    gap: 0;
    color: #071d25;
    font-family: var(--milaura-font-body);
    font-size: clamp(3.05rem, 16vw, 4.65rem);
    font-weight: 500;
    line-height: 0.94;
    letter-spacing: 0;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-headline span {
    color: inherit;
    display: block;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-headline .hero-home-headline-accent {
    display: inline-flex;
    width: fit-content;
    margin-top: 2px;
    padding: 0.03em 0.16em 0.09em;
    border-radius: 12px;
    background: linear-gradient(135deg, #53dce8, #f5d27e);
    color: #071d25;
    font-family: var(--milaura-font-heading);
    font-weight: 700;
    line-height: 0.92;
    box-shadow: 0 16px 34px rgba(28, 110, 125, 0.18);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
    max-width: 22rem;
    margin: 18px 0 0;
    color: rgba(7, 29, 37, 0.86);
    font: 700 0.98rem/1.42 var(--milaura-font-body);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-action-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-top: 22px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price {
    order: 2;
    color: rgba(7, 29, 37, 0.88);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price-label {
    color: rgba(7, 29, 37, 0.70);
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-price-now {
    font-size: 1rem;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-cta {
    order: 1;
    width: min(100%, 294px);
    min-height: 52px;
    justify-self: start;
    padding: 15px 22px;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-trust {
    display: flex;
    margin-top: 16px;
    gap: 8px;
    color: rgba(7, 29, 37, 0.82);
    font-size: 0.72rem;
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-trust span {
    min-height: 30px;
    padding: 8px 10px;
    border: 1px solid rgba(255, 255, 255, 0.62);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.42);
    backdrop-filter: blur(12px);
  }

  #MilauraHeroHome-{{ section.id }} .hero-home-trust span:nth-child(n+3) {
    display: none;
  }

  body:has(#MilauraHeroHome-{{ section.id }}) #milaura-bubble-label {
    display: none !important;
  }

  body:has(#MilauraHeroHome-{{ section.id }}) #milaura-bubble-trigger {
    bottom: calc(var(--milaura-active-dock-height, 84px) + 124px + env(safe-area-inset-bottom)) !important;
    right: 15px !important;
  }

  @media (max-width: 379px) {
    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      padding-left: 14px;
      padding-right: 14px;
      padding-bottom: calc(96px + env(safe-area-inset-bottom));
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: clamp(2.72rem, 15vw, 3.4rem);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
      font-size: 0.9rem;
    }
  }

  @media (min-width: 768px) {
    #shopify-section-{{ section.id }}.milaura-hero-homepage {
      margin-top: calc(-1 * var(--header-height-tablet, 132px)) !important;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      padding: calc(var(--header-height-tablet, 132px) + 48px) 48px 74px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      height: 100%;
      display: grid;
      grid-template-columns: minmax(0, 0.98fr) minmax(360px, 0.72fr);
      align-items: end;
      gap: 32px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-content {
      grid-column: 2;
      width: min(100%, 560px);
      justify-self: start;
      padding-bottom: 6px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-img {
      object-position: left center;
    }

    body:has(#MilauraHeroHome-{{ section.id }}) #milaura-bubble-label {
      display: block !important;
    }

    body:has(#MilauraHeroHome-{{ section.id }}) #milaura-bubble-trigger {
      bottom: clamp(36px, 4.8vw, 72px) !important;
      right: clamp(34px, 4.8vw, 78px) !important;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-stone-tag {
      top: auto;
      right: clamp(34px, 5vw, 86px);
      bottom: clamp(160px, 17vw, 240px);
      min-height: 40px;
      padding: 9px 14px;
      font-size: 0.76rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-ring-cutout {
      position: absolute;
      z-index: 2;
      display: block;
      width: clamp(130px, 13vw, 238px);
      height: auto;
      right: clamp(40px, 7vw, 120px);
      bottom: clamp(40px, 7vw, 92px);
      filter: drop-shadow(0 24px 34px rgba(8, 35, 43, 0.20));
      transform: rotate(7deg);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-headline {
      font-size: clamp(4.4rem, 7.8vw, 8.2rem);
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-subtitle {
      max-width: 31rem;
      font-size: 1.08rem;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-action-row {
      grid-template-columns: auto 1fr;
      align-items: center;
      margin-top: 28px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-price {
      order: 2;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-cta {
      width: auto;
      min-width: 236px;
    }
  }

  @media (min-width: 1100px) {
    #shopify-section-{{ section.id }}.milaura-hero-homepage {
      margin-top: calc(-1 * var(--header-height-desktop, 166px)) !important;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-shell {
      padding: calc(var(--header-height-desktop, 166px) + 46px) 70px 76px;
    }

    #MilauraHeroHome-{{ section.id }} .hero-home-inner {
      grid-template-columns: minmax(520px, 1.02fr) minmax(440px, 0.66fr);
      gap: 54px;
    }
  }
{% endstyle %}

<div id="MilauraHeroHome-{{ section.id }}" aria-label="{{ hero_label }}">
  <div class="hero-home-shell">
    <div class="hero-home-inner">
      <div class="hero-home-content">
        {%- if section.settings.badge_text != blank or section.settings.show_stock -%}
          <div class="hero-home-badge-row">
            {%- if section.settings.badge_text != blank -%}
              <span class="hero-home-badge">{{ section.settings.badge_text }}</span>
            {%- endif -%}
            {%- if section.settings.show_stock -%}
              <span class="hero-home-stock">{{ section.settings.stock_count }} restantes</span>
            {%- endif -%}
          </div>
        {%- endif -%}

        {%- if section.settings.pretitle != blank -%}
          <p class="hero-home-pretitle">{{ section.settings.pretitle }}</p>
        {%- endif -%}

        <h1 class="hero-home-headline">
          <span>{{ headline_line_1 }}</span>
          <span>{{ headline_line_2 }}</span>
          <span class="hero-home-headline-accent">{{ headline_line_3 }}</span>
        </h1>

        {%- if section.settings.subtitle != blank -%}
          <p class="hero-home-subtitle">{{ section.settings.subtitle }}</p>
        {%- endif -%}

        <ul class="hero-home-benefits" aria-label="Bienfaits de la pierre">
          {%- if section.settings.benefit_1_text != blank -%}
            <li>
              <span class="hero-home-benefit-icon" aria-hidden="true">{{ section.settings.benefit_1_icon }}</span>
              <span>{{ section.settings.benefit_1_text }}</span>
            </li>
          {%- endif -%}
          {%- if section.settings.benefit_2_text != blank -%}
            <li>
              <span class="hero-home-benefit-icon" aria-hidden="true">{{ section.settings.benefit_2_icon }}</span>
              <span>{{ section.settings.benefit_2_text }}</span>
            </li>
          {%- endif -%}
          {%- if section.settings.benefit_3_text != blank -%}
            <li>
              <span class="hero-home-benefit-icon" aria-hidden="true">{{ section.settings.benefit_3_icon }}</span>
              <span>{{ section.settings.benefit_3_text }}</span>
            </li>
          {%- endif -%}
        </ul>

        <div class="hero-home-action-row">
          {%- if product != blank -%}
            <div class="hero-home-price" aria-label="Prix">
              <span class="hero-home-price-label">{{ section.settings.price_label }}</span>
              <span class="hero-home-price-now">{{ product.price | money }}</span>
              {%- if product.compare_at_price > product.price -%}
                <span class="hero-home-price-was">{{ product.compare_at_price | money }}</span>
              {%- endif -%}
            </div>
          {%- endif -%}

          <a href="{{ cta_url }}" class="hero-home-cta">
            <span>{{ section.settings.cta_text | default: 'Decouvrir' }}</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M5 12h14"></path>
              <path d="m13 6 6 6-6 6"></path>
            </svg>
          </a>
        </div>

        {%- if section.blocks.size > 0 -%}
          <div class="hero-home-trust">
            {%- for block in section.blocks -%}
              {%- if block.type == 'trust' -%}
                <span {{ block.shopify_attributes }}>{{ block.settings.icon }} {{ block.settings.label }}</span>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>

      <div class="hero-home-visual-col">
        {%- if hero_image != blank -%}
          <div class="hero-home-visual">
            <picture class="hero-home-picture">
              <source media="(max-width: 767px)" srcset="{{ 'milaura-hero-aigue-marine-chloe-mobile-v1.webp' | asset_url }}">
              <img
                class="hero-home-img"
                src="{{ 'milaura-hero-aigue-marine-chloe-v1.webp' | asset_url }}"
                alt="{{ section.settings.image_alt | default: product.title | default: section.settings.headline | strip_html | escape }}"
                width="1672"
                height="941"
                loading="eager"
                fetchpriority="high"
              >
            </picture>
            {%- if section.settings.product_note != blank -%}
              <span class="hero-home-stone-tag">
                <span class="hero-home-stone-dot" aria-hidden="true"></span>
                {{ section.settings.product_note }}
              </span>
            {%- endif -%}
            <img
              class="hero-home-ring-cutout"
              src="{{ 'milaura-aigue-marine-ring-cutout-v1.webp' | asset_url }}"
              alt=""
              width="1122"
              height="1402"
              loading="eager"
              aria-hidden="true"
            >
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Hero Homepage",
  "tag": "section",
  "class": "milaura-hero-homepage",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Produit mis en avant"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image (fallback si pas de produit)"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge (vide = cache)",
      "default": "Produit de l'ete"
    },
    {
      "type": "text",
      "id": "pretitle",
      "label": "Accroche courte",
      "default": "La pierre de l'ete est arrivee chez MilAura"
    },
    {
      "type": "inline_richtext",
      "id": "headline",
      "label": "Titre principal",
      "default": "Aigue-marine"
    },
    {
      "type": "text",
      "id": "headline_line_1",
      "label": "Titre ligne 1",
      "default": "Je suis"
    },
    {
      "type": "text",
      "id": "headline_line_2",
      "label": "Titre ligne 2",
      "default": "la bague"
    },
    {
      "type": "text",
      "id": "headline_line_3",
      "label": "Titre ligne 3",
      "default": "de l'ete"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Texte alternatif image campagne",
      "default": "Chloe porte la bague Aigue-marine MilAura"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Une bague en argent 925 et aigue-marine AA, pensee pour ramener calme, clarte et legerete dans vos journees."
    },
    {
      "type": "text",
      "id": "benefit_1_icon",
      "label": "Icone benefice 1",
      "default": "✦"
    },
    {
      "type": "text",
      "id": "benefit_1_text",
      "label": "Benefice 1",
      "default": "Apaise le mental"
    },
    {
      "type": "text",
      "id": "benefit_2_icon",
      "label": "Icone benefice 2",
      "default": "✧"
    },
    {
      "type": "text",
      "id": "benefit_2_text",
      "label": "Benefice 2",
      "default": "Clarifie les pensees"
    },
    {
      "type": "text",
      "id": "benefit_3_icon",
      "label": "Icone benefice 3",
      "default": "☼"
    },
    {
      "type": "text",
      "id": "benefit_3_text",
      "label": "Benefice 3",
      "default": "Allege la charge mentale"
    },
    {
      "type": "text",
      "id": "product_note",
      "label": "Pastille visuelle produit",
      "default": "Aigue-marine AA"
    },
    {
      "type": "text",
      "id": "price_label",
      "label": "Label prix",
      "default": "Bague"
    },
    {
      "type": "checkbox",
      "id": "show_stock",
      "label": "Afficher compteur stock",
      "default": false
    },
    {
      "type": "number",
      "id": "stock_count",
      "label": "Stock restant",
      "default": 30
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Texte du bouton",
      "default": "Decouvrir la bague"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "URL du bouton (vide = lien produit)"
    }
  ],
  "blocks": [
    {
      "type": "trust",
      "name": "Badge confiance",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icone",
          "default": "💎"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Texte",
          "default": "Pierre naturelle certifiee"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Homepage",
      "blocks": [
        { "type": "trust", "settings": { "icon": "💎", "label": "Aigue-marine AA" } },
        { "type": "trust", "settings": { "icon": "🇫🇷", "label": "Selection MilAura" } },
        { "type": "trust", "settings": { "icon": "🚚", "label": "Livraison offerte des 19,90€" } }
      ]
    }
  ]
}
{% endschema %}
