# Checkout MilAura V2 live, 2026-09-13 10:46 CEST

## Statut final

LIVE VERIFIE.

- profil checkout actif : `MilAura Checkout UX 2026-09-13` ;
- identifiant du profil actif : `9395241307` ;
- ancienne configuration `Ma boutique` conservee comme brouillon de repli ;
- theme public : `190430282075` ;
- source : branche `codex/milaura-urgent-ux-20260912`, commit fonctionnel `cf95fe34` ;
- aucune commande et aucun paiement effectues pendant la recette.

## Publication

- preflight du fichier live `locales/fr.json` effectue avant ecriture ;
- la seule difference preflight correspondait aux sept libelles checkout valides ;
- push cible de `locales/fr.json` avec `--only`, `--nodelete`, `--allow-live` et `--strict` ;
- pullback du theme live identique octet pour octet a la source ;
- publication du profil confirmee dans Shopify Admin par `Configuration publiee` ;
- Shopify Admin affiche `MilAura Checkout UX 2026-09-13` comme `Actif` et `Ma boutique` dans les brouillons.

## Contenu live

- `Paiement express avec vos informations enregistrees` ;
- `Ou continuer sans compte` ;
- `Vos coordonnees` ;
- `J ai deja un compte MilAura` ;
- `Comment souhaitez-vous etre livre ?` ;
- `A domicile` ;
- `En point relais` ;
- recapitulatif Aigue-marine ecume `#DCEBE8` ;
- accents et actions Encre prune `#2F222D` ;
- logo vers `https://milaura.fr/` et icone panier vers `https://milaura.fr/cart`.

Les apostrophes et accents typographiques sont presents dans Shopify. Ils sont simplifies ici uniquement pour respecter la convention ASCII des documents du depot.

## Verification publique

- vrai checkout public ouvert sans parametre `preview_theme_id` ni `profile_preview_token` ;
- mobile controle a 390 x 844 px, sans debordement horizontal ;
- desktop controle a 1440 x 1000 px, sans debordement horizontal ;
- fond Aigue-marine ecume calcule present en `rgb(220, 235, 232)` ;
- accents Encre prune calcules presents en `rgb(47, 34, 45)` ;
- sept libelles attendus retrouves dans le checkout public ;
- logo boutique et lien panier confirmes dans le DOM ;
- journal navigateur : aucune erreur ;
- Shopify Theme Check avant publication : 0 erreur et 16 avertissements historiques hors du fichier modifie.

## Limite conservee

Le forfait Basic ne permet pas d ajouter un lien texte libre `Retour au panier` dans le checkout. Les deux sorties natives restent actives : logo vers la boutique et icone vers le panier. Aucun contournement fragile ni application supplementaire n a ete installe.

## Suite

Mission Checkout fermee. La prochaine mission autorisee par Patrice est le correctif du Hero mobile deja prepare dans ce worktree. Sa publication reste un gate distinct.
