# C1 O2 : commandes privees validees et rollback ferme

Date : 2026-08-25 12:28 CEST

Statut : `FERME - O2 PASS PRIVE - ROLLBACK PASS - AUCUNE RELEASE`

## Verdict master

Le master accepte le retour :

`O2_PASS_PRIVATE_ORDERS_QA - ROLLBACK_PASS`

Le commit prive `e863fc100ccae47ff1c8a43cdfeccef763f4bcd4` ajoute uniquement le rapport expurge `docs/milaura/shopify-admin-canonical/c1-o2-orders-private-qa/2026-08-25-c1-o2-orders-private-qa.md`.

La branche `codex/milaura-c1-o2-orders-private-qa-20260825` et son worktree sont propres, synchronises `0/0`. Aucun listener local `3017`, `3457` ou `3018` n est actif.

## Preuves acceptees

- O2-0 a O2-7 PASS ;
- schema Admin GraphQL valide avant mutation ;
- cinq contrats GraphQL et gabarits de variables figes et haches ;
- aucun champ `code` utilise dans les selections executees ;
- un seul produit QA et un seul variant crees puis publies uniquement sur Online Store du dev store ;
- une seule commande test associee a RNO1, `PAID`, `FULFILLED`, inventaire `BYPASS` ;
- notifications commande et fulfillment a `false` ;
- Mail read-only : `O2_MAIL_ZERO_MESSAGES` ;
- etats `orders-no-diagnostic` puis `complete` valides ;
- mapping par handle, media, pierre favorite Obsidienne noire et destinations coherents ;
- responsive 360, 390, 430 et 1440 px, sans overflow ;
- clavier, focus visible et console C1 valides ;
- diagnostic purge sans resurrection ;
- confirmation destructive Patrice recue avant suppression ;
- commande et produit supprimes chacun par une seule mutation, sans retry ;
- produit, variant, media, handle, publication et commande absents apres nettoyage ;
- RNO1 revenu a zero commande ;
- `backend_url` vide, flag theme `false`, theme `205027279193` toujours non publie ;
- `write_products`, `write_publications` et `write_orders` absents ;
- configuration canonique restauree ;
- App Dev, backend et listeners arretes ;
- fichiers temporaires et prives supprimes.

## Impact Release Candidate

- RC1 : `PASS` ;
- RC2 : `PASS TECHNIQUE` ;
- RC3 : `PASS PRIVE` ;
- RC4 : `PASS PRIVE` ;
- RC5 : `PARTIEL`, attend verite inventaire, cout, marge, catalogue et destinations ;
- RC6 : audit ferme avec `NO-GO CONDITIONNEL`, shell natif Orders et Profile encore en anglais ;
- RC7 : `PASS PRIVE O2` ;
- RC8 : `PASS PRIVE` avec et sans commande ;
- RC9 : `PASS` ;
- RC10 : reaudit selectif `PASS`.

Le Release Candidate global reste ouvert. O2 ne donne aucun droit app deploy, release, Admin, bascule Customer Accounts, integration theme, publication ou live.

## Prochain ordre C1

1. Geler O2 a `e863fc10`. Aucun nouveau produit ou commande de test n est necessaire.
2. Attendre la verite inventaire et catalogue finalisee par Patrice avant de reserver RC5. Le lot RC5 devra auditer le mapping produit et variant vers pierre, les destinations publiques, les produits sans pierre, retours, annulations, remboursements, cadeaux et egalites, sans modifier le catalogue sans GO separe.
3. Traiter ensuite le shell natif francais et la coordination Mail dans un lot Admin distinct. Aucun template ou notification Mail ne peut etre modifie par C1.
4. Une fois RC5 et le francais fermes, reserver la productionisation : backend stable, secrets, retention, observabilite, backup et rollback, puis app deploy ou release privee sous GO distinct.
5. L integration theme, la bascule Admin, la QA publique et le live restent quatre gates separees.

Aucun nouveau lot C1 n est reserve dans ce checkpoint. La session C1 peut se fermer apres handoff. Le prochain cadrage appartient au master lorsque la verite inventaire est disponible.
