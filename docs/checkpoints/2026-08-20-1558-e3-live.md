# E3 - Lifecycle actif - GO LIVE execute

Date : 2026-08-20 15:58 CEST

Statut : nouveau checkout E3 actif, ancien checkout inactif, rappel de panier Shop inactif. Canari initial passe, surveillance 24 h ouverte.

## Autorisation et perimetre

- autorisation explicite recue : `GO LIVE E3` ;
- mutation limitee au rappel de panier Shop et aux deux parents checkout Shopify Messaging ;
- aucun fichier theme, produit, compte client ou consentement modifie ;
- aucune permission Klaviyo acceptee ;
- E4+, comptes live, cross-sell live, Atelier des emotions et ScratchToReveal exclus.

## Verification avant mutation

- checkout theme canonique propre sur `codex/milaura-integration` au commit `eefdd4fb1390c4145147629de0070de84a4b84c0`, identique au distant ;
- worktree prive propre sur `codex/milaura-e3-lifecycle-actif-20260820` au commit `c8067b98624c1770016da1c02af92f2c9ac06aef`, identique au distant ;
- proprietaire E3 confirme dans `docs/workstreams.md` ;
- base E1 privee `0478c2820e7a11aebb51e8535245566a4cfe20a7` conservee ;
- dernier audit reel Klaviyo du 2026-08-20 a 13:48 CEST : un seul flow manuel `Sq5fbd`, aucun flow consultation, panier ou checkout ;
- les onglets de demande d extension d autorisations Klaviyo sont restes intacts et non acceptes.

## Basculement live effectue

1. `Notifications client > Remarketing avec Shop > Rappel de panier` passe de coche a decoche ; confirmation `Parametres enregistres` observee.
2. `Retour en stock`, `Baisse de prix` et `Abandon de navigation` laisses inchanges.
3. Ancien parent checkout `68816896347`, activite `204165415259`, passe de `Actif` a `Inactif`.
4. Nouveau parent certifie `73633464667`, activite `210569822555`, passe de `Inactif` a `Actif`.
5. Workflow actif : `01a01f0e-0022-79fa-bd86-9754f5185a4c`, version `01M0FGW0127DAJB5VN1RSN9DYD`.

## Preuves apres bascule

La liste Shopify Messaging confirme :

- consultation `72198390107` active ;
- panier `68816961883` actif ;
- nouveau checkout `73633464667` actif ;
- ancien checkout `68816896347` inactif ;
- archive checkout inactive ;
- exactement un parent checkout actif.

Le nouveau checkout affichait zero envoi au moment de l activation. Le canari relu apres activation est reste stable, sans erreur visible ni parent checkout concurrent. Aucun email de test ou client n a ete declenche par cette operation.

Le rappel de panier Shop a ete relu apres le swap et reste decoche. Les trois autres options Shop visibles sont restees cochees et n ont pas ete modifiees par E3.

## Source privee et validations

- branche privee : `codex/milaura-e3-lifecycle-actif-20260820` ;
- commit live pousse : `33fe905a2d24e6a74153b27f599e6d22306df1a1` ;
- preuve : `docs/milaura/shopify-admin-canonical/e3-lifecycle/snapshots/2026-08-20/flow/01a01f0e-0022-79fa-bd86-9754f5185a4c/live-evidence.json` ;
- `scripts/verify_baseline.py` passe pour les trois parcours et l etat checkout certifie actif ;
- quatre JSON parses ;
- `git diff --check` passe ;
- aucun identifiant client versionne.

## Rollback pret

1. desactiver `73633464667` ;
2. verifier `68816896347` et son activite `204165415259` ;
3. reactiver `68816896347` seulement apres confirmation que `73633464667` est inactif ;
4. restaurer le rappel de panier Shop seulement si la politique de deduplication est explicitement abandonnee ;
5. ne jamais laisser les deux parents checkout actifs simultanement.

## Suivi restant

- surveiller pendant 24 h les runs, erreurs et doublons ;
- ne pas ouvrir E4 avant la prochaine gate du plan canonique ;
- le test reel de desabonnement reste distinct car il exige un envoi controle ;
- ne pas basculer les comptes live avant preview et tests de parite.
